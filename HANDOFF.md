# HANDOFF — 퀀트 트레이딩 시스템 인수인계

> 다른 Claude 세션/계정이 이 프로젝트를 이어받을 때 **이 문서부터 읽으면** 바로 따라잡습니다.
> 사용자: 한국 개인 투자자(KOSPI/KOSDAQ 단기매매).

## ★ 우선순위 (중요)
- **주력 = `macro_watcher.py`** — 텔레그램 자동 신호 봇 + 복기/학습 루프. **여기가 메인 작업 대상.**
- **부차 = `quant_dashboard.py`** — Streamlit 대시보드. 텔레그램에서 **실전 검증 끝난 것만** 나중에 반영. 대시보드에 먼저 힘 빼지 말 것.
- 신규 아이디어·강의·복기 학습은 **먼저 macro_watcher에 반영 → --analyze/실전 검증 → (검증되면) 대시보드**.

---

## 1. 구성 파일 (어디가 최신인지 주의)

| 파일 | 역할 |
|---|---|
| `macro_watcher.py` | **핵심.** 상시 감시 → 텔레그램 자동 신호(매크로·종배·눌림·시가배팅·돌파·시간외·과매도낙주·재료투매·짝꿍/상따 관찰·단기스윙·공시·보유관리·저녁브리핑·마감복기) |
| `quant_dashboard.py` | Streamlit 대시보드(종배 원톱·내일후보·돌팬티 관제판·머니투어 등) |
| `tools_gui.py` | Tkinter GUI 실행기(감시 시작/중지·각 기능 버튼·보유종목 관리·시장복기 보기) |
| `my_holdings.json` | 보유종목(손절/익절·8시 홀딩판정). **현재: 알테오젠(196170)·원익(032940)·서산(079650)** |
| `my_watch.json` | 관심종목(내관심 타점) |
| `manju_watchlist.json` | 대시보드 라인업 |
| `rules_ledger.md` | **규칙 원장** — 강의·매매일지·복기에서 뽑은 규칙과 반영/폐기 이력(중복방지·추적) |
| `market_review.md` | **시장 복기 원장** — 매일 마감복기 + 과거 backfill 누적(학습 플라이휠) |
| `learning_review_prompt.md` | 강의/일지 → 코딩가능 규칙 추출용 프롬프트(타 AI 붙여넣기용) |
| `signal_scorecard.json`·`pick_history.json` | 신호/픽 성적 로그(런타임 생성, PC마다 다름·gitignore) |

⚠️ **최신 소스는 "사용자 PC의 파일"입니다.** 이번 세션 커밋이 GitHub에 아직 안 올라갔을 수 있음(아래 6번 참조).

---

## 2. 텔레그램 신호 (고수 강의 9강 + 학습 반영)

강의 9개(관심종목/종가배팅/시간외/시가배팅/돌파/눌림/짝꿍/상따/단기스윙)를 코드화:

| 신호 함수 | 시간대 | 요지 |
|---|---|---|
| `check_dolpanty_pick` (종배) | 15:05~/NXT 18~20 | 거래대금·20MA·비과열·브리핑테마·**수급·재무(적자)·전고점·테마대장/2등**·익일무효화. **재료 미확인=점수상한49(원톱 배제)**, **팩트체크 강등종목 회피** |
| `check_pullback_scan` (눌림) | 09:05~15:20 | 정배열 지지반등·**수급 게이트(필수)**·프로그램 전환·평단 아래·my_watch 중복제외 |
| `check_opening_bet` (시가배팅) | 09:00~09:10 | 4유형(장전뉴스/해외발/시간외연장/갭하락과매도)·시초 프로그램·시초가 이탈손절·물타기금지 |
| `check_breakout` (돌파) | 09:11~15:00 | 전고점/라운드피겨/신고가+거래량2배+프로그램·하루2회상한·저갭억제 |
| `check_afterhours` (시간외) | 17:00~18:30 | NXT 10분 실체결델타 3억↑·강한재료 |
| `check_oversold_bounce` (과매도낙주) | 지수-1.3%↓ 급락일 | 대형주 낙폭과대+수급+저가반등 |
| `check_material_washout` (재료투매반등) | 09:10~14:40 | 강재료주 +5%후 눌림·당일청산 |
| `check_swing_scan` (단기스윙) | 15:00~15:25 | 정배열+수급연속+상대강세+**공급악재(유증·CB) 배제** |
| `check_rsi_cmf_macd_recovery` (RSI·CMF·MACD 3중반등) | 09:05~15:20 | **RSI(Wilder14) 과매도(≤30)반등 + CMF20(OBV대체) 3일연속상승 + MACD 저점(-10↓)→-5통과→0회복**, 3개 전부 동시충족(AND)해야 발송 + 거래대금30억↑·거래량5일평균1배↑ 게이트. indicators.py 재사용(신규 재구현 안 함) |
| `check_a_grade_pullback` (A급 눌림·5분봉재돌파) | 09:15~15:00 | 사용자 요청(퍼플렉시티 상담 검토) — **당일 지지존(전일종가·시가·파동50%·VWAP 中 2개↑ 겹침)에서 5분봉 재돌파+거래량 재증가**로만 발송. 당일+5~15%(15%초과 추격금지)·재료확인(S/A/T)·거래대금100억↑·코스피급락(-1%↓) 아님. 매수확정 아님(알림·자동매매 없음) |
| `check_limitup_follow`·`check_pair_trade` (상따/짝꿍) | 장중 | **관찰용(SIG_WATCH)** — 실시간 상한가잔량/VI는 API 한계라 근사 |
| `check_dart_disclosures` | 07:00~20:00 | 공시 호재/악재. **🔄자사주 반전 신호**(급락-2%↓ + 자기주식**취득**; 해지·처분 제외) |
| `check_holdings` | 상시 | 손절/익절 알림(근접·이탈 통합 쿨다운)·8시 NXT 홀딩판정(재료A급 단독 매도 안 함) |
| `check_evening_news` (저녁브리핑) | 17~22시 | RSS(연합+한경+이데일리)→Gemini 브리핑+**구글검색 팩트체크(grounding)**·**뉴스 화제성 랭킹**·**네이버 많이 본 뉴스(실제 클릭 랭킹, 텔레그램에도 별도 표기)**·📌주목테마TOP3가 `save_auto_lineup`으로 내일 라인업 확정 |
| `check_morning_lineup_finalize` (아침 라인업 최종점검) | 07:00~08:35 | 저녁픽은 미국장 열리기 전(선물) 데이터라, 개장 전 **실제 마감 실측+간밤 신규뉴스**로 한 번 더 검증 — 새 종목 추가 없이 기존 라인업 안에서 유지/제외만 판단(Gemini). 키 없으면 간밤 악재 종목만 기계적 제외(최소 안전장치) |
| `send_daily_review` (마감복기) | 15:35~50 | 신호총정리 + **Gemini 시장복기(왜 움직였나)→market_review.md 누적** |

**매크로 킬스위치** `compute_macro`: 미국 나스닥선물·SOX·WTI + 코스피(KIS). sev 0/1/2(양호/중립/리스크오프). **미국 휴장 감지**(NQ·WTI 둘다 0 → 판단보류). data outage시 sev2 fail-safe.

---

## 3. 학습 루프 (이 세션의 핵심 성과)

```
강의/매매일지/HTS  →  규칙 추출(learning_review_prompt)  →  rules_ledger 적립  →  코드 반영  →  --analyze/실전 검증
과거·매일 마감  →  backfill/마감복기(Gemini grounding)  →  market_review.md 누적  →  패턴 학습  →  신규 신호(예: 자사주 반전)
```
- `--backfill-review N` : 과거 N거래일 시장 복기 미리학습(Gemini 웹검색). yfinance 지수 불안정이라 제거, grounding이 숫자·원인 직접 검색.
- 검증된 학습 1호 = **자사주 반전 신호**(복기 8/25·9/1·9/3 "급락→자사주매입→V자" 3회 반복 → 코드화 → 테스트로 해지 오판 버그 잡고 수정).

---

## 4. 성적 검증

- `--analyze` : 신호종류별 누적 승률(signal_scorecard.json)
- `--report` : 추천 성적표. **종가승률 + 🎯장중 익절도달률**(장중 +3% 찍었는지 = 규칙대로면 성공) 둘 다 표시.
- `--exit-analysis` : 종배 청산 타이밍(시가 vs 종가·NXT별)
- 실측 경향(이 장세): **브리핑/종배 승률 높음(~55~64%), 아침 당일단타 낮음(17~23%, 반전장 취약)** → 브리핑·종배 위주 권장.

---

## 5. 알려진 한계 (반드시 인지)

1. **실시간 호가창·상한가 잔량·VI 없음** — KIS 실시간 웹소켓 미연동. 짝꿍·상따는 근사(관찰용).
2. **과거 장중 수급·거래대금 랭킹 소급 불가** — 신규 신호(수급·프로그램 의존)는 백테스트 불가, **포워드(--analyze)로만 검증**.
3. **yfinance 지수(^KS11/^KQ11) 불안정** — 'possibly delisted' 빈발. 지수는 KIS(_kospi_index_kis/_index_snapshot) 우선.
4. **Gemini grounding 무료쿼터** — 연속 호출(backfill 10연타)시 실패 가능. 12초 재시도·6초 간격 넣음. 일부 과거일자 '미래날짜'로 착각하는 응답은 `_is_future_confused()`로 감지해 스킵(저장 안 함).
5. **개인 순매수는 근사**(=-(외인+기관)). 기타법인/자사주 수급 직접 필드 미사용.
6. **재료 등급**(_news_grade)은 네이버 뉴스 제목 기반 → 6일전 계약 등 못 잡을 수 있음. 그날 최강등급 유지(강등차단)로 안정화.
7. **`_index_daily_range`(KIS 지수 일별시세, FHKUP03500100) 필드명 미검증** — 실제 KIS 키로 한 번도 테스트 못 함(이 세션엔 키 없음). `stck_bsop_date`/`bstp_nmix_*` 필드명은 다른 KIS 지수 엔드포인트 관례를 따른 추정. 응답은 왔는데 파싱이 0건이면 콘솔에 `[지수일별시세 진단]`으로 실제 필드명을 찍으니, 실키로 `--backfill-review` 처음 돌릴 때 로그 확인 필요. 틀렸으면 그 로그의 필드명으로 `_index_daily_range` 수정.
8. ✅ **`_naver_ranking_news`(네이버 '많이 본 뉴스') 실사용 검증 완료** — 사용자 PC 실행 결과로 15건 정상 파싱 확인(2026-09-08). 첫 실행에서 HTML 엔티티 미디코딩(`&hellip;`·`&ldquo;` 등 그대로 노출)과 `<a>` 안 중첩 태그(말줄임 아이콘 등)에서 제목이 잘리는 두 버그 발견 → `html.unescape()` + `(.*?)` DOTALL 캡처 후 태그 제거로 수정 완료.

---

## 6. ⚠️ GitHub 상태 (인수인계 핵심)

- 이번 세션 **약 150+ 커밋이 로컬에만** 있었고, 세션 내내 **푸시가 403(guy1715-svg에 Claude GitHub App 미설치)**로 막힘.
- 따라서 **GitHub 기본 브랜치는 이번 세션 작업 이전 상태일 수 있음.** 작업 브랜치: `claude/amazing-hamilton-gxccom`.
- **동기화 필수**: ①`guy1715-svg`에 Claude GitHub App 설치(https://github.com/apps/claude/installations/select_target) 후 push, 또는 ②사용자 PC에서 직접 `git push`.
- 동기화 전엔 **사용자 PC의 파일이 최신**(세션 중 SendUserFile로 전달·다운로드됨).

---

## 7. 다음 작업(대기)

- ✅ ~~`market_review.md` 며칠 누적되면 → 주1회 "최근 장세 패턴 요약"(메타 복기) 생성~~ — 구현 완료(`weekly_meta_review`, 아래 참조).
- ⏳후보: 자사주/기타법인 **실시간 수급** 직접 추적(현재 DART 공시 기반), 팩트체크 국내지수 실측 대조.
- 신규 신호(시간외·시가배팅·돌파·과매도낙주·재료투매·스윙) **--analyze 승률 검증** 후 임계 튜닝.
- 대시보드: 신규 신호 반영은 실전 검증 후로 보류(현재 텔레그램 전용).
- ⏳보류(구조적으로 어려움, 9/18 갭상승 역분석 AI 제안 중 미착수분): ①장전 증권사 신규
  커버리지·목표주가 상향 리포트 실시간 크롤링(한라캐스트·삼성전기 케이스) — 국내 증권사
  리포트를 실시간으로 안정적으로 긁어올 공개 API가 없어 스크레이핑 신뢰성 문제로 보류.
  ②해외 이벤트(임상/학회 등)→국내 공급망 기업 매핑(파미셀·대한광통신 케이스), ③대장주
  갭업 시 동일 세부테마 2등주 자동 추적(삼화콘덴서 케이스) — 둘 다 현재 `SECTORS`/
  `SECTOR_LEADERS`가 6대 거시테마(반도체·2차전지·바이오·방산·원전·인터넷)만 다루고
  MLCC 같은 세부 산업 단위 짝 매핑이 없음 — 새로 만들면 케이스별로 끼워맞추기(과최적화)
  위험이 커서, 이런 사례가 반복 확인될 때만 신중히 추가하기로 함.
- ⏳후보: `📊 성과 분석` 탭(`_compute_signal_stats`)과 `🎯 종배픽·재료등급` 탭도 로컬
  `signal_scorecard.json` 전체 누적본을 직접 읽는 구조라, 클라우드 배포판에서는 다일치 승률
  통계가 비어있을 수 있음(7.45 참고 — signal_log는 당일치만 스냅샷에 실림). 다일치 통계까지
  클라우드에서 보려면 `signal_scorecard.json` 자체(또는 최근 N일 슬라이스)를 GitHub data
  브랜치에 추가로 올리는 조치가 필요 — 아직 미착수.

### 7.51 시가배팅에 "거래량 급증"(재료 무관) 진입 경로 추가

**배경**: 사용자가 "갭상승 뜨고 거래량뜨고 시초가 위치로 타점잡는거 설정해줘"라고 요청. 확인해보니
이 3가지 조건(갭상승·거래대금·시초가 위치)은 이미 `check_opening_bet`(시가배팅)의 핵심 게이트였고,
추가로 "재료(뉴스/공시) 확인"이 필수 조건으로 걸려있었음. 사용자에게 "기존 시가배팅 조정 vs 완전
새 스캐너" 중 택일을 물어 **기존 조정**으로 확정(새 스캐너를 또 만들면 PR #20에서 정리한 중복신호
문제가 재발할 위험).

**수정**: `check_opening_bet`의 갭상승(①②③) 분기에 "거래량 급증"(재료 무관) 진입 경로를 추가 —
`_vol_ratio_5d`(기존 함수 재사용, 오늘 거래량/5일평균 배수)로 배수를 구하고, `check_vol_surge`가
이미 쓰고 있는 "2배 이상" 기준을 그대로 재사용. 다만 무근거 추격을 막기 위해 `_is_theme`(테마
등급)와 동일한 원칙으로 수급 또는 프로그램 매수 뒷받침을 같이 요구(`_prog_ok or _supply_ok`).
메시지에 "📊거래량 N.N배(5일평균)" 태그 추가. 판정 태그는 재료가 없는 경로라 `_verdict_tag`가
이미 자동으로 C(정량 근거만 있음)로 매김 — 로직 변경 없음.

**검증**: `py_compile` 통과. 3개 시나리오(재료없음+거래량1.5배→기존처럼 스킵/재료없음+거래량2.5배
+프로그램매수→신규 경로로 발송/재료S등급+거래량0.5배→기존처럼 발송, 회귀 없음) 시뮬레이션 테스트
통과. 갭하락(유형④) 분기가 새 변수(`_vol_mult`) 때문에 NameError 나지 않는지 별도 확인.

### 7.50 signal_log 날짜 미초기화 — 스냅샷에 어제 신호가 "오늘 신호"로 잘못 실릴 수 있던 문제

**배경**: 사용자가 대시보드 "오늘 신호 타임라인"을 보고 "이거 오늘 꺼 맞아?"라고 질문 — 확인
결과 실제 버그가 있었음. `state["signal_log"]`는 `_log_signal()`이 **새 신호를 기록할 때만**
그 시점에 날짜를 비교해서 초기화되는 구조였는데, 스냅샷(`snap["signal_log"]`)에 담는 코드
(main 루프)는 이 값을 그냥 그대로 복사만 하고 날짜를 다시 확인하지 않았음. 그래서 오늘 아직
새 신호가 하나도 안 뜬 시점(장 시작 전, 또는 신호가 아예 없는 조용한 날)엔 어제 신호 목록이
그대로 남아있는 채로 "오늘 신호"라고 스냅샷에 실려 대시보드(오늘 신호 타임라인·분석저널
자동채우기)에 표시될 수 있었음. 바로 아래 `alert_feed`는 이미 스냅샷을 만들 때마다 직접
날짜를 대조하는 올바른 패턴을 쓰고 있어서, 같은 파일 안에서도 일관성이 없었던 사각지대.

**수정**: `snap["signal_log"]` 할당부에 `alert_feed`와 동일한 날짜 대조 로직 추가 —
`state["signal_log"]["_day"]`가 오늘과 다르면 빈 리스트로 처리.

**검증**: `py_compile` 통과. 3개 시나리오(어제 데이터 잔존+오늘 신규신호 없음→빈 리스트/
오늘 신규신호 있음→정상표시/signal_log 자체 없음(최초실행)→빈 리스트) 시뮬레이션 테스트 통과.

### 7.49 마감복기 결론이 "데이터 장애"를 "진짜 리스크오프"로 오인하던 문제 수정

**배경**: 사용자가 보내준 9/18(금) 마감 리포트 확인 — 매크로 국면 마감이 "🔴 데이터 outage ·
신규매수 보수적 차단(지표 조회 실패)"였는데도, 그날 실제로는 시가저격(두산)·진입(SK하이닉스)·
A급(SK하이닉스·한화에어로스페이스) 신호가 다 떴고 라인업 성적도 SK하이닉스 +6.42%로 좋았음.
그런데 맨 아래 "오늘 결론"은 "🔴 리스크오프 — 무포지션이 정답"이라고 나와서, 실제 그날 결과와
정반대되는 코칭을 하고 있었음. 코드 확인 결과 `send_daily_review`의 결론 분기가 `sev_hi >= 2`
여부만 보고, 그 sev=2가 **진짜 리스크오프**인지 **지표 조회 자체가 실패한 데이터 장애**
(`compute_macro`의 fail-safe, `check_dolpanty_pick`은 이미 이 둘을 구분해서 씀)인지는 전혀
구분하지 않고 있었음.

**수정**: `journal_accumulate`가 그날 `sev_hi`를 갱신하는 순간마다 그 원인이 데이터 outage였는지
(`macro.text`에 "outage" 포함 여부)를 `j["outage_at_hi"]`에 같이 기록하도록 변경. `send_daily_review`
결론 분기에 이 플래그를 반영 — 진짜 리스크오프(`_outage=False`)는 기존 "무포지션이 정답" 문구
그대로 유지하고, outage로 인한 sev=2는 신호 발생 여부에 따라 "장애로 신규매수만 막혔을 뿐,
신호는 정상 발송 — 라인업 성적으로 실제 결과 확인" 또는 "장애로 판단 못 내린 날" 문구로 분리.

**검증**: `py_compile` 통과. 5개 시나리오(outage+신호있음/outage+신호없음/진짜리스크오프/정상신호일/
무신호일) 시뮬레이션 테스트 — 기존 3개 케이스는 문구 그대로 유지되고, outage 2개 케이스만 새
문구로 분리되는 것 확인.

### 7.48 DART "시설투자" 공시 사각지대 수정 — 갭상승 역분석(9/18) 가온전선 사례

**배경**: 사용자가 보내준 "오늘 갭상승 원인 역분석"(자동 발송 기능, 이전 세션에서 구현)에서
가온전선(+6.0%)이 "전날 미리 감지 가능했다(장마감 후 대규모 미국 설비투자 공시)"고 나왔음.
코드 확인 결과 `check_dart_disclosures`는 공시 제목이 `_DART_POS`(호재)/`_DART_NEG`(악재)/
`_DART_PERF`(실적)/`_DART_AGM_KW`(임시주총) 중 어디에도 안 걸리면 조용히 `continue`로
건너뛰는데, "시설투자결정"이라는 DART 표준 공시명이 `_DART_POS`에 아예 없어서 다른 재료
유형(공급계약·수주·특허 등)은 다 처리되는데 이것만 사각지대로 빠지고 있었음.

**수정**: `_DART_POS`에 "시설투자", "타법인주식및출자증권취득" 추가. 금액 기반 세부 등급
(대형/중형 구분)은 하지 않음 — `_dart_material_grade`의 계약금액 파싱(`_contract_detail`)이
공급계약 공시 서식 전용이라 시설투자 서식에 그대로 재사용하면 파싱 실패 위험이 있어, 일단
뉴스등급 기반 기본 판정(🟢보통재료)으로 유입만 시키는 최소 수정으로 범위를 제한함.

**검증**: `py_compile` 통과. 4개 케이스(시설투자 공시 처리됨/타법인지분취득 처리됨/정기주총은
여전히 스킵/기존 악재 키워드 유지) 시뮬레이션 테스트 통과.

**함께 검토했으나 미착수**: 같은 리포트가 제안한 나머지 5개 종목(한라캐스트·파미셀·
대한광통신·삼성전기·삼화콘덴서)의 개선안은 위 "다음 작업" 섹션에 보류 사유와 함께 기록함
(증권사 리포트 크롤링·해외이벤트-공급망 매핑·세부테마 2등주 추적 — 전부 새 데이터소스나
케이스별 큐레이션이 필요해 신중 검토 필요).

### 7.47 AI 초안 파싱 강건성 개선 — 일부 항목만 채워지던 문제

**배경**: 7.46 배포 후 사용자가 실제로 "AI로 초안 채우기" 버튼을 눌러보니, 경고 문구는 뜨는데
(초안 자체는 생성됨) 사건/전이경로 등 텍스트박스가 비어있는 채로 남는 현상 발견. 원인 —
`_ai_journal_draft`의 정규식이 `"사건: ..."` 형식만 정확히 매칭했는데, Gemini가 마크다운
굵은 글씨(`**사건**:`)나 줄머리 불릿(`- 사건:`)을 섞어 응답하면 매칭이 깨져 그 항목만
빈 채로 남았음.

**수정**: 마크다운 강조 기호(`**`, `*`) 제거 후 파싱, 줄머리 불릿(`-`·`•`·`·`) 허용, 줄 시작
기준(`re.MULTILINE`) 매칭으로 관대화. 그래도 5개 항목을 전부 못 찾으면(형식이 완전히 다른
경우) 빈 화면 대신 AI 원문을 "사건" 칸에 그대로 넣어줘 최소한 사용자가 눈으로 보고 직접
정리할 수 있게 안전장치 추가.

**검증**: `py_compile` 통과. 3개 시나리오(마크다운+불릿 혼합/콜론 형식 다양/완전히 다른
포맷→원문 폴백) 단위테스트 통과. `streamlit run --headless` 기동 200 OK·헬스체크 정상.

### 7.46 분석저널에 AI 초안 생성 버튼 추가 — "나는 아직 그정도가 아니야" 반영

**배경**: 7.44~7.45로 종목/가격 자동채우기까지는 됐는데, 사건/전이경로/산업/기업/반대근거는
여전히 빈 텍스트박스였음. 사용자에게 "직접 채우기 vs AI 초안 생성" 중 선택지를 제시했더니
"모두 AI 넣게 해줘 나는 아직 그정도가 아니야"로 명확히 응답 — 처음 설계(수기 작성으로 분석력
훈련) 의도보다 사용자의 현재 필요(우선 완성된 형태를 보고 배우기)를 우선하기로 함.

**수정**: `_ai_journal_draft(pf)` 신규 — 선택한 신호에 대해 `mw._gemini_grounded`(기존 함수 재사용,
구글검색 grounding으로 최신 실제 뉴스 확인 후 생성 — 순수 생성형 환각 방지)를 호출해 5개 항목을
고정 형식으로 받아 파싱. `render_journal()`에 "🤖 AI로 초안 채우기" 버튼 추가 — 누르면 5개
텍스트박스가 채워지고, 상시 "⚠️ AI가 생성한 초안입니다 — 검증되지 않았으니 저장 전에 꼭
확인·수정하세요" 경고를 같이 표시(그대로 저장을 막지는 않되, AI 추정임을 계속 눈에 띄게 함).
Gemini 키 없거나 응답 실패 시 예외 없이 에러 메시지만 표시.

**검증**: `py_compile` 통과. 파싱 로직(5개 항목 정상 추출, "확인 안 됨" 케이스 포함) 단위테스트
통과. `streamlit run --headless` 기동 200 OK·헬스체크 정상.

### 7.45 대시보드 "오늘 신호" 표시가 클라우드 배포에서 항상 비어있던 문제 — GitHub 스냅샷으로 전환

**배경**: 7.44에서 만든 "오늘 신호에서 자동 채우기"를 사용자가 직접 확인해보니 목록이 항상
비어있었음. 원인 파악 결과 — 대시보드가 **주소로 접속하는 클라우드 배포판**(Streamlit
Community Cloud)이었는데, `render_home`(오늘 신호 타임라인)·`render_journal`(자동채우기)·
`render_settings`(감시 프로세스 상태)가 전부 `mw.SCORECARD_FILE`/`mw.STATE_FILE`을 **로컬
파일 경로로 직접 읽고** 있었음. 이 파일들은 사용자 PC에서 `macro_watcher.py`가 쓰는 파일이라
클라우드 서버(별개의 컨테이너)에서는 애초에 존재할 수 없는 구조 — 이건 이번 세션에서 새로
만든 버그가 아니라 대시보드 V26 전면 재구축(7.x 이전) 때부터 있던 사각지대였고, "오늘 신호에서
자동 채우기"를 붙이면서 실사용 확인 과정에서 처음 발견됨.

**수정**: `macro_watcher.py`는 이미 매 루프 `push_snapshot_github()`로 GitHub `data` 브랜치에
`snapshot.json`을 올리고 있고(사용자 로컬 `run_gui.bat`에 `GITHUB_TOKEN` 설정돼 있어 실제로
`data` 브랜치에 커밋이 쌓이고 있는 것을 `git pull` 로그로 확인함), 그 안에 `signal_log`(당일치
신호, KST 기준)와 `updated_ts`(감시 최근 갱신 시각)가 이미 포함돼 있었음. 새 업로드 로직을
추가하는 대신, 대시보드 쪽에 `_fetch_snapshot()`(GitHub Contents API로 `data` 브랜치의
snapshot.json을 읽어옴, 60초 캐시) + `_today_signal_log()`(그 중 `signal_log`만 추출)를 추가해
기존 세 곳(오늘 신호 타임라인·분석저널 자동채우기·감시 프로세스 상태)이 전부 이걸 쓰도록
전환. 스냅샷을 못 가져와도(토큰 없음·네트워크 오류) 예외 없이 빈 값으로 조용히 폴백하고,
감시 프로세스 상태는 로컬 파일 확인도 폴백 경로로 남겨둬 로컬 실행 환경도 계속 지원.

**검증**: `py_compile` 통과. `_fetch_snapshot`/`_today_signal_log` 로직 3개 시나리오(정상 파싱/
404 폴백/네트워크 오류 폴백) 단위테스트 통과. `streamlit run --headless` 기동 200 OK·헬스체크
정상.

### 7.44 분석저널 탭 — "오늘 신호에서 자동 채우기" 추가(수기입력 부담 완화)

**배경**: 7.42에서 만든 분석저널 탭을 실제로 열어본 사용자가 "이걸 내가 다 넣으라고?"라고 반응 —
사건·전이경로·산업·기업·반대근거·주가 6개 텍스트박스가 전부 빈칸으로 보이면 입력 부담이 커
보이는 게 정당한 피드백이었음(실제로는 종목명만 있으면 저장은 되지만 UI가 그렇게 안 보였음).

**수정**: `render_journal()`에 "오늘 신호에서 자동 채우기" 선택박스 추가 — 이미 모든 신호 함수가
공통으로 쌓는 `signal_scorecard.json`(새 파일 아님, 기존 재사용)에서 오늘 발생한 신호 목록을
시간 역순으로 보여주고, 하나 고르면 종목명·코드와 "신호종류·시각·진입가"가 자동으로 채워짐.
나머지 분석 필드(사건/전이경로/산업/기업/반대근거)는 여전히 비워둬도 저장 가능 — 상단 안내
문구를 "종목+판정만 고르고 바로 저장해도 됨"으로 명확히 함. 선택 라벨을 위젯 key에 포함시켜
다른 신호를 고르면 입력값이 새로 반영되게 함(Streamlit 위젯 상태 재설정 패턴).

**검증**: `py_compile` 통과. 자동채우기 로직(오늘/이전 날짜 필터링, 시간 역순 정렬, 기본값 생성)
단위테스트 통과. `streamlit run --headless` 기동 200 OK·헬스체크 정상.

### 7.43 감시 중복실행 방지 락 + 서산·머큐리 NXT 경보 재발 — 세션별 유예 분리

**배경**: 사용자 제보 두 건이 같은 날 연달아 들어옴. ①"오늘의 복기" 메시지가 매일 두 번씩
온다("대시보드때문에 오는거같은데") → 확인 결과 `quant_dashboard.py`는 신호를 전혀 계산·발송하지
않는 순수 뷰어라 원인이 아니었고(`send_telegram`/`send_daily_review` 등 호출 자체가 코드에 없음),
실제 원인은 `tools_gui.py`로 감시를 켠 상태에서 콘솔로 `macro_watcher.py`를 또 실행하거나 GUI를
재시작해 감시 프로세스가 중복으로 떠 있을 때 각 프로세스가 같은 시각에 각자 알림을 보내는
구조적 결함이었음(중복실행을 막는 장치 자체가 없었음). ②"서산·머큐리 시세 조회 3회 연속 실패"
경보가 PR #14(7.30 참조)로 고친 뒤에도 16:04(NXT 애프터 개장 직후)에 재발 — 원인 재분석 결과
`holdings_nxt_seen`이 하루 통틀어 종목당 키 1개만 써서, **아침 프리(8시)에 유효가를 한번 봤다는
이유로 저녁 애프터(16~20시) 개장 직후의 정상적인 호가공백까지 유예 없이 바로 실패로 세고
있었음**(프리·애프터가 서로 다른 세션인데 하나로 뭉뚱그린 게 남아있던 결함).

**수정**:
- `_acquire_watch_lock()`/`_touch_watch_lock()`/`_release_watch_lock()` 신규 + `macro_watcher.lock`
  파일(mtime 기반 하트비트). 감시 루프(`while True`) 진입 직전 1회 획득 시도 — 이미 신선한(600초 내
  갱신된) 락이 있으면 "중복 실행 감지" 경보 1번 보내고 즉시 종료. 윈도우 `TerminateProcess`(GUI
  "감시 중지" 버튼)는 정상종료 훅을 안 태우므로 존재 여부가 아니라 **신선도**로 판정 — 강제종료
  후에도 600초 지나면 다음 실행이 스스로 회복됨(수동 삭제 불필요). `--stock`/`--report` 등
  일회성 CLI 명령은 락 대상 아님(상시 루프에만 적용).
- `check_holdings`의 `_nxt_seen` 키를 `code` 단독에서 `f"{code}_{세션}"`(pre/aft)으로 분리 — 프리와
  애프터를 별개 세션으로 취급해 각자 첫 유효가를 볼 때까지는 그 세션 안에서만 유예.

**검증**: `py_compile` 통과. 락 메커니즘 5단계 시나리오(최초획득/중복차단/하트비트/강제종료 후
자동회복/정상해제) 전부 통과. NXT 세션 분리 3단계 시나리오(프리에서 기록 → 애프터 개장 직후
3틱 실패해도 경보 안 뜸(재발 버그 재현·수정 확인) → 애프터에서 이미 본 뒤 진짜 장애는 정상
경보) 전부 통과.

### 7.42 판정(A/B/C/D) 태그 + 이벤트 브리핑 + 분석저널 — 사용자 학습 프레임 반영

**배경**: 사용자가 외부(Perplexity) FOMC 시황 글을 보고 "사건→전이경로→산업→기업→가격"으로
연결하는 분석법을 배우고 싶다고 요청. 대화 중 구체적 개선안 3가지를 직접 제안: ①목표주가를
숫자 아닌 가정(이익추정·배수·기간) 3개로 받고 가장 약한 가정이 깨졌을 때 재계산 ②이 판정기준을
텔레그램 메시지에도 적용 ③FOMC/CPI 이벤트 체크포인트를 미리 발송. 세 가지 모두 적용 요청.
사용자가 "종목선정까지 물 흐르듯"을 요청했을 때, 거시 서사만으로 종목을 새로 찍어내는 예측
로직은 추가하지 않고(구조적으로 불가능함을 재확인) 기존 검증된 스캐너 재사용으로만 연결함.

**수정**:
- `_verdict_tag(level, price_bad, is_bad, quant_basis)` 신규 — 재료 직접성(S/A/T/none, 기존
  `_news_grade` 체계 재사용) + 가격자리(과열/낙폭과대 여부)로 A/B/C/D 한 글자 판정. 기존 재료등급
  계산을 새로 만들지 않고 그대로 재사용.
- `check_dart_disclosures`·`check_opening_bet`·`check_dolpanty_pick`의 텔레그램 메시지에
  `·판정{A~D}` 태그 삽입(9개 지점) — 사용자가 대시보드 분석저널에 적을 '판정'과 바로 대조 가능.
- `check_macro_event_brief()` 신규 — 기존 `_overnight_event_risk`(종배 게이트용으로 이미 있던
  이벤트 감지)를 재사용해, FOMC/CPI 등 감지 시 마감복기 시점(15:35~15:50) 1회 "동결=매수·인상=매도
  이분법 금지, 4가지(정책결정/성명서/점도표/시장반응) 확인" 체크리스트를 발송. 추가로 `_volume_rank`+
  `_news_grade`+`_ma20_disparity`(전부 기존 함수) 재사용해 "오늘 조건 충족 후보" 스냅샷을 참고용으로
  붙임(확정픽 아님, 예측 아님 — 최종 매수는 기존 종배픽/시가배팅 게이트 통과 필요).
- `quant_dashboard.py`에 "📝 분석 저널" 탭 신규 — 사건/전이경로/산업/기업/반대근거/주가/판정(A~D)
  순서로 입력받아 `analysis_journal.json`에 저장(관심종목과 동일한 GitHub Contents API 자동반영
  패턴). 판정 필터로 과거 기록 조회 가능. 신호 계산에는 관여하지 않는 순수 기록소.

**검증**: `py_compile` 양쪽 통과. `_verdict_tag` 9개 케이스 단위테스트 전부 통과. 
`check_macro_event_brief` mock 5개(gemini키없음 스킵/시간창밖 스킵/이벤트감지시 발송+state기록/
같은날 중복방지/이벤트없음시 미발송) 전부 통과. `quant_dashboard.py`는 `streamlit run --headless`로
기동해 200 OK·헬스체크 정상 확인(KIS/Firebase 키 없는 환경에서도 예외 없이 렌더). 분석저널
JSON 저장/로드 왕복 테스트 통과.

### 7.41 주간 신호 성적표 자동발송 추가 — "계속 지켜보기만 할거야?" 요청 반영

**배경**: 사용자가 실제 `--report`(최근 1~2일 스냅샷)와 `--analyze`(과거 전체 누적) 결과를
직접 보내줬는데, 그동안 이걸 볼 방법이 tools_gui 버튼을 사용자가 직접 눌러야만 하는 것뿐이었음
— "한 달치 데이터가 쌓였으면 방향을 잡았어야지, 계속 지켜보기만 할거냐"는 지적. 실제로
`--analyze`가 이미 있었지만 **버튼을 눌러야만 보이니 "보는 걸 잊으면 데이터가 쌓여도 아무도
안 보는" 구조**였음.

**분석(사용자가 보내준 실측 --analyze 결과, 참고용 기록)**: 종배그림자(68건·51%·-0.1%)가
사실상 "이 장세에서 아무 종목이나 사면 나오는 기준선"에 가깝고, 브리핑(61건·49%·**+0.8%**)은
승률은 기준선과 비슷해도 평균수익이 기준선보다 훨씬 좋아 표본이 충분한 것 중 유일하게 확실히
기대값 플러스로 확인됨 → **브리핑은 유지·집중 권장**. 종배픽(NXT)(6건·50%·-2.1%)·종배분산
(6건·33%·-1.9%)·시간외단일가(2건·0%·-1.2%)·과매도낙주(1건·-7.5%)는 표본은 작지만 지금까지
일관되게 안 좋음 → 표본 더 쌓일 때까지 소액·주의 권장. 나머지(진입·눌림타점·시가저격·조기포착
등)는 표본 10건 미만이라 판단 보류가 맞음(사용자 데이터 자체에 이미 이 경고가 붙어 있었음).

**수정**: 신규 `weekly_signal_report()` — `weekly_meta_review`와 동일 주기(매주 금요일
15:35~15:50)로 `_analyze_history`(신호별 누적 승률) + `_analyze_exit_timing`(청산타이밍 분석)을
**자동으로** 텔레그램 발송. 이제부터 사용자가 기억해서 버튼을 누르지 않아도 매주 성적표가
저절로 옴 — "승률 확인"이 습관이 아니라 시스템의 일부가 되도록.

**검증**: `py_compile` 통과. mock 시나리오 4개 — 금요일 15:40+KIS키있음 → 안내+분석+청산분석
3건 발송 / 같은 주 재호출 → 중복 발송 안 됨 / 월요일 → 스킵 / KIS 키 없음 → 스킵. 전부 통과.

### 7.40 전면 감사 — 신호 12개 중복·충돌 정리(사용자 "답답하다, 근본적으로 뭐가 문제냐" 요청)

**배경**: 사용자가 여러 세션에 걸쳐 요청이 들어올 때마다 신호를 하나씩 추가해온 결과에 강하게
반발 — "거래량 보고 뉴스보고 타점 잡아주는 게 그렇게 어렵냐. 니가 부족한 거 아니냐. 현재 로직을
전부 하나도 놓치지 말고 확인해서 엉킨 부분을 다 풀고 간결하게 만들어라"는 요청. 서브에이전트로
`macro_watcher.py`의 장중 스캐너 12개(check_pullback_scan·check_rsi_cmf_macd_recovery·
check_a_grade_pullback·check_material_washout·check_my_watch·check_breakout·check_early_catch·
check_bar15·check_oversold_bounce·check_swing_scan·check_dolpanty_pick·check_dart_disclosures 등)를
전수 감사(코드 줄 단위로 대조).

**감사로 확인된 실제 문제 4가지**:
1. **같은 종목에 알림 3~4건 중복 발송 가능** — "눌림·반등" 현상을 5개 함수가 각자 다른 수학
   (일봉MA5/MA20·RSI+CMF+MACD·5분봉VWAP·고점대비 눌림·관심종목MA5)으로 따로 검사하고, 상호
   중복배제 코드가 my_watch 관련 2줄뿐이었음 — 조건 맞는 종목 하나가 "🎯눌림타점"+"📐RSI·CMF·
   MACD반등"+"⚡재료투매반등"을 동시에 받을 수 있었음. "돌파" 계열도 4개 함수가 같은 구조.
2. **과열(20일선 이격) 기준이 함수마다 7%/10%/12%로 제각각** — 이격 +8~11% 종목에 한 함수는
   "추격금지", 다른 함수는 "지금 매수"를 동시에 보낼 수 있었음.
3. **코스피 급락일 판정 기준 충돌** — `check_oversold_bounce`는 -1.3%부터 "지금이 기회"로
   활성화, `check_a_grade_pullback`은 -1.0%부터 "신규 단타 금지"로 정지 — 두 임계 사이·그 이하
   구간에서 정반대 메시지가 동시에 나갈 수 있었음.
4. **`check_bar15` 성적표 기록 버그** — 실제로 텔레그램이 발송되는 "주도주" 분기엔 `_log_signal`
   호출이 없고, **발송이 억제된 "비주도주" 분기에만** 있어서, "15분봉" 신호의 성적표 데이터가
   실제 발송 이력과 정반대로 쌓이고 있었음.

**수정(각 함수의 판정 로직 자체는 안 건드림 — 함수를 통합·삭제하지 않고, 중복·모순만 제거)**:
- 신규 `_claim_daily_signal(state, category, code, now_kst)` — 공용 "그날 이 종목·이 카테고리를
  누가 먼저 잡았는지" 락. "pullback"(눌림 5종)·"breakout"(돌파 4종) 두 카테고리에 적용, 먼저
  조건을 충족한 함수만 발송하고 나머지는 같은 기회를 다시 알리지 않음. `check_breakout`처럼
  자체적으로 "하루 2회까지" 재돌파를 허용하는 함수는 그 종목의 '첫' 발생 때만 다른 함수와
  경쟁하고, 이후 자기 재돌파는 그대로 허용(자기 몫은 유지).
- `DISP_WARN=7.0`·`DISP_BLOCK=12.0` 전역 상수 신설 — 기존에 이미 있던 `_deep_stock`(종목해석)의
  라벨 기준(과열=7%·심한과열=12%)에 나머지 13곳(check_pullback_scan·_verify_news_picks 등에
  남아있던 "10%" 두 곳 포함)을 전부 맞춤. 새 숫자를 만들지 않고 기존 관례로 통일.
- `check_a_grade_pullback`의 코스피 급락 정지선을 -1.0%→-1.3%로 올려 `check_oversold_bounce`의
  활성화 기준과 일치시킴(퍼플렉시티 조건의 취지는 유지 — 급락 시 "무작정 신규 단타"만 멈추고,
  그 이하 구간은 oversold_bounce의 엄격 게이트 통과분만 예외로 허용하는 구조 유지).
- `check_bar15`의 `_log_signal` 호출을 실제 발송(주도주) 분기로 이동.

**의도적으로 안 한 것**: 5개 눌림 함수·4개 돌파 함수를 물리적으로 1개씩으로 합치는 건 이번엔
안 함 — 각자 서로 다른 방법론(일봉MA/RSI·CMF·MACD/5분봉VWAP 등)으로 같은 현상을 감지하는
것 자체는, 어느 방법이 실제로 잘 맞는지 데이터가 더 쌓이면 비교·선택할 수 있는 자산이기도
함(각자 다른 kind로 로깅되니 `--analyze`로 방법론별 승률 비교 가능). 지금 당장 사용자가 겪는
고통(중복·모순 알림)은 락으로 해결되고, 함수 통합은 --analyze 데이터로 "이 방법이 진짜 낫다"가
확인된 뒤에 하는 게 더 안전하다고 판단.

**검증**: `py_compile` 통과. 기존에 이 세션에서 작성한 mock 테스트 10개 전부 재통과(회귀 없음
확인) + 신규 교차검증 테스트 — 같은 종목·같은 state에서 `check_pullback_scan`이 먼저 발송하면
`check_rsi_cmf_macd_recovery`가 같은 종목에 대해 조건을 전부 충족해도 발송 안 함(중복락 정상
작동) / 서로 다른 종목이면 둘 다 정상 발송(락이 과잉작동해 무관한 종목까지 막지는 않음) 확인.

### 7.39 A급 눌림 매매 신규 추가 — 퍼플렉시티 상담 검토 후 구현 + RSI·CMF·MACD 진단 보강

**배경1(신규 기능)**: 사용자가 퍼플렉시티와 상담한 단타 매매타점 전략(장문)을 공유하며 "전문가
입장에서 검토하고, 가능하면 대시보드·텔레그램 신호로 넣어달라" 요청. 검토 결과 — 원칙 자체
(승률보다 기대값, 전략 하나만 검증, 20회는 문제발견용)는 이미 봇의 기존 철학과 대부분 겹쳤지만,
**"일봉 이동평균 대신 당일 형성된 지지존(전일종가·시가·오늘 첫파동 50%되돌림·VWAP)에서 5분봉
재돌파+거래량 재증가로 진입 확인"**은 기존에 없던 방식이라 신규 기능으로 구현할 가치가 있다고 판단.

**구현**: 신규 `_intraday_snapshot()`(inquire-price 1회 호출로 현재가·시가·고가·저가·누적거래량·
누적거래대금·VWAP 전부 추출) + `check_a_grade_pullback()` — check_bar15와 동일한 '스냅샷을
5분 버킷으로 누적'하는 근사 기법(진짜 캔들 API 아님)으로 5분봉을 흉내내고, 직전 버킷 대비
고점 갱신+거래량 재증가를 "재돌파"로 판정. 종목조건(퍼플렉시티 원문 그대로): 당일 +5~15%
(15%초과 추격금지), 재료확인(S/A/T 중 하나, 무재료 제외), 거래대금 100억+ 유지, 코스피 급락일
(-1%↓) 아님. 지지존은 전일종가·당일시가·파동50%·VWAP 중 2개 이상이 현재가 ±1.5% 이내로
겹칠 때만 인정. 매수확정 아닌 알림용(자동매매 없음 — 퍼플렉시티·기존 봇 원칙과 동일), 1분봉
정밀 진입확인은 사용자 몫으로 안내. `_log_signal()`로 로깅해 **대시보드 코드 수정 없이** 기존
"오늘 신호 타임라인"·"성과 분석(승률)" 탭에 자동으로 함께 집계됨(재사용 원칙 그대로 적용).

**배경2(진단 보강)**: 어제 만든 RSI·CMF·MACD 3중 검색기가 하루 만에 신호 0건이라 사용자가
"왜 안 왔는지 분석해"라고 요청. 확인해보니 이 함수엔 다른 신호들과 달리 "0건 진단 로그"가
아예 없었음(7.32에서 4개 함수에 추가할 때 이 함수는 그보다 나중에 만들어져서 빠짐) — 그래서
"돌았는데 조건이 안 맞았다"인지조차 알 방법이 없었음.

**수정**: RSI/CMF/MACD 각 단계 통과 종목 수를 세는 카운터 추가 — 0건일 때
"1차후보 N종 중 RSI조건통과 N종 → CMF조건통과 N종 → MACD조건통과 N종(그중 악재제외 N종) →
발송 0건"처럼 **어느 단계에서 대부분 걸러지는지**까지 보이게 함(단순 0건보다 훨씬 정보량 많음).
로직 자체는 3조건 AND(사용자가 직접 선택한 방식)라 원래도 드문 조합 — 하루 조용했다고 고장은
아니지만, 이제부턴 로그로 확인 가능.

**검증**: `py_compile` 통과. `check_a_grade_pullback` mock 시나리오 4개 — ①지지존 2개 겹침+
5분봉 재돌파(거래량 재증가 포함) → 정상 발송 ②당일 +15% 초과 종목 → 회귀 제외 확인
③무재료 종목 → 회귀 제외 확인 ④코스피 급락(-1.5%) → 회귀 신규단타 보류 확인. `check_rsi_cmf_
macd_recovery`는 기존 3-AND-게이트 회귀 시나리오 5개 그대로 재통과 확인(카운터 추가가 판정
로직 자체엔 영향 없음).

**참고**: `_intraday_snapshot()`이 사용하는 `acml_vol`(누적거래량) 필드는 이 세션에 KIS 실키가
없어 실물 응답으로 검증은 못 했음(다만 이미 daily-price 응답에서 동일 필드명이 검증된 이력
있음 — 표준 KIS 스키마이므로 현재가 조회에도 있을 가능성이 높음). 사용자 로그에서 `[A급눌림]`
줄이 매 사이클 정상적으로 찍히는지(오류 없이) 확인해주면 필드명 이슈 여부까지 확정 가능.

### 7.37 뉴스 소스 운영 가이드 반영 — 밤사이 FOMC·CPI 이벤트 차단(종배) 신규 추가

**배경**: 사용자가 "네이버 뉴스는 탐색용, DART/KIND는 확정용" 등 뉴스 소스 운영 원칙을 담은 상세
가이드 문서를 전달(직접성 등급 A~D, 신선도 24시간 규칙, 선반영 필터, 밤사이 이벤트 차단 등 7개
항목). 오늘(9/16 저녁)이 마침 실제 FOMC 발표일(9/17 새벽 3시 KST)이라 특히 시급한 요청.

**기존에 이미 구현돼 있던 부분(재구현 안 함)**: 코드를 확인해보니 가이드의 상당 부분이 **이전
세션에서 이미 반영돼 있었음**:
- 직접성 등급 A~D·테마 연결 금지(해외 대기업 투자→국내 종목 자동 연결 금지 등)·선반영 판정·
  뉴스 발생일 기준 신선도 판정 — `_gemini_factcheck()`의 프롬프트(중요1~7)에 이미 상세히 명시돼
  있고, 저녁 브리핑(`check_evening_news`)이 매일 이 팩트체크를 거쳐 `state["factcheck_avoid"]`로
  강등/제외 종목을 저장 → `check_dolpanty_pick`이 이 목록을 종배 후보에서 자동 배제(`_fc_avoid`).
- 선반영 필터(가이드: +10%↑·이격+15%↑ 추격금지) — 기존 종배 로직이 이미 **더 보수적으로**
  등락 +7%↑·이격 +7%↑에서 컷하고 있어 가이드 기준보다 엄격함. 추가 조정 불필요.
- 네이버=탐색/DART=확정 원칙 — `check_dart_disclosures`(공시 기반 신호)와 `check_evening_news`
  (네이버/RSS 기반 테마 탐색)가 이미 별도 파이프라인으로 분리돼 있음.

**이번에 새로 추가한 것 — 밤사이 핵심이벤트 차단(가이드 #6, 기존에 없던 유일한 항목)**: 신규
`_overnight_event_risk()` — Gemini+구글검색(grounding, 기존 `_gemini_grounded()` 재사용)으로
"지금부터 내일 개장 전까지 FOMC/CPI/PPI/고용지표/연준인사 연설/초대형기업 실적 중 확정 예정이
있는지" 실제 검색 확인. `check_dolpanty_pick`의 확정픽 강등 게이트에 추가 — 이벤트가 있으면
재료·수급이 아무리 좋아도 확정픽(매수) 대신 "🌒[종배·관망] 확정픽 강등" 메시지로 전환하고
그림자(검증용 관찰목록)로만 로깅. 키 없음/검색 실패 시 보수적으로 이벤트 없음 처리(과잉 억제 방지).

**검증**: `py_compile` 통과. mock 시나리오 4개 — ①`_overnight_event_risk` 이벤트 있음 파싱
②이벤트 없음 파싱 ③검색 실패시 보수적 폴백 ④키 없음시 폴백. 통합 시나리오 2개 — ⑤이벤트
리스크 있음(FOMC) → 확정픽 대신 관망 강등 메시지, 실제 매수 확정픽은 발송 안 됨 확인 ⑥이벤트
없음 → 기존과 동일하게 정상 확정픽 발송(회귀 확인). 전부 기대대로 통과.

**의도적으로 손 안 댄 것(인프라 부족)**: 가이드 #1의 "A급은 반드시 DART/KIND 원문으로 확정"을
`_news_grade()`(Naver 키워드 매칭) 자체에 강제하려면 종목코드(6자리)↔DART corp_code(8자리)
매핑이 필요한데, 이건 DART가 별도 제공하는 전체 상장사 매핑 파일(corpCode.xml, 수천 건)을
받아서 캐싱하는 인프라가 추가로 필요함 — 이번엔 손 안 댐. 대신 이미 존재하는
`check_dart_disclosures`(공시 직접 감시)와 `_gemini_factcheck`(간접 검증)이 이 역할을 상당 부분
대신하고 있음. 필요하면 다음에 별도로 구축 가능.

### 7.36 근본원인 분석 — "재료 뉴스등급"이 테마·정책 재료를 아예 인식 못 하던 구조적 결함

**배경**: 사용자가 7.35의 "일부만 고쳤다"는 답변에 반발 — "근본적으로 왜 못찾는건데? 원인을
찾아서 분석하고 실행해야지." 다시 파고들어 진짜 근본원인을 찾음.

**원인**: `check_opening_bet`(시가배팅, 09:00~09:10)은 `_volume_rank`로 시장 전체 거래대금
상위를 라이브로 스캔하니 그날 갭상승 6종 다수(금호전기·한국첨단소재·가온전선 등, 정책/테마
재부각 성격)를 **거래대금으로는 이미 포착**했을 가능성이 높음. 그런데 진입후보로 인정하려면
재료 확인(`_ng in ("S","A")`) or 어제 브리핑 픽 or 해외발 동조 중 하나가 필요한데, `_news_grade`의
S/A 키워드가 "공급계약·실적·임상" 등 **직접 계약/실적류만** 인식하고 "테마·클러스터·정책수혜·
사업목적변경" 같은 키워드는 전혀 모름 — 그래서 실제로 거래대금이 몰리고 있었어도 재료 게이트에서
전부 탈락, 시가배팅이 못 잡았을 것으로 추정(사용자 PC 실제 로그로 최종 확인 필요 — 재현 환경엔
KIS 실키가 없어 오늘 그 종목들이 실제로 거래대금 상위 40위 안에 들었는지까지는 못 봄).

**수정**: `_news_grade()`에 S/A와 완전히 분리된 신규 등급 **T(테마·정책)** 추가
(`_NEWS_T_KW`: 테마·수혜주·클러스터·정책수혜·사업목적·사명변경 등) — 기존 S/A 로직·다른 소비함수는
전혀 안 건드림(모두 `_ng in ("S","A")`로 명시 비교라 T는 자동으로 무해). `check_opening_bet`에만
T등급을 새 진입조건으로 추가하되, S/A보다 근거가 약하므로 **수급 유입 or 프로그램 매수 뒷받침이
있을 때만** 인정(무근거 테마 추격 방지) — "테마·정책 재부각" 유형으로 별도 표시.

**검증**: `py_compile` 통과. mock 시나리오 4개 — ①`_news_grade`가 테마 키워드에 T 반환 ②기존 공급계약
키워드는 여전히 S 반환(회귀) ③T등급+프로그램매수 뒷받침 있음 → 시가배팅 포착(신규 동작 확인)
④T등급인데 수급/프로그램 뒷받침 전혀 없음 → 여전히 필터링(무근거 추격 방지 확인) ⑤기존 S등급
경로 회귀 확인. 전부 기대대로 통과.

**한계/다음 단계**: 이 세션엔 KIS 실키가 없어 "오늘 그 6종목이 실제로 거래대금 상위 40위 안에
들어서 시가배팅이 스캔은 했는데 재료게이트에서만 걸렸다"는 것까지는 실측으로 확인 못 함(추정).
사용자 PC 로그에서 `[시가배팅]` 라인에 이 종목명이 "후보"로라도 잡혔었는지 확인해주면 이 가설이
맞는지 검증 가능. T등급은 이번엔 `check_opening_bet`에만 적용 — 필요하면 `check_pullback_scan`·
`check_breakout` 등 다른 스캐너에도 같은 방식으로 확장 가능(우선 가장 영향 큰 지점 하나만 반영).

### 7.35 DART 임시주총 안건 감지 추가 — "주주총회"가 전부 스킵되던 사각지대

**배경**: 9/16 09:03·09:04에 실제로 받은 "오늘 갭상승 원인 역분석"(`check_gap_analysis`)에서 그날
갭상승 6종이 전부 어제 브리핑/종배픽에 없던 종목(0/6 적중)으로 나왔고, 그중 **빛과전자(+8.0%)**의
원인 분석에 "임시주총 안건(사명변경 등)으로 전날 미리 감지 가능했음"이라는 문구가 있었음. 사용자가
"오늘도 다 놓쳤네?"라고 지적. 코드를 확인해보니 실제로 `check_dart_disclosures`의 `_DART_SKIP`
목록에 "주주총회"가 통째로 들어있어서, 사명변경·신규사업목적 추가 같은 실질적 재료가 담긴
**임시주총 공시까지 정기주총(매년 형식적 결산 안건)과 함께 전부 무시**되고 있었음 — 구조적 사각지대.

**주의**: 이 수정은 "오늘도 다 놓친" 근본 원인(저녁 브리핑·종배픽의 종목 선정 자체가 이 6종을
못 골랐던 것) 전체를 해결하는 건 아님 — 그건 매일 수천 종목 중 내일 갭상승할 종목을 하루 전에
전부 맞히는 문제라 원천적으로 매우 어려움(성적 검증에도 "아침 당일단타 낮음 17~23%"로 이미 기록돼
있음). 오늘 고친 건 그중 **"임시주총 공시로 사전에 감지 가능했는데 코드가 아예 안 보고 있던"**
구체적이고 확인된 한 가지 사각지대만 — 나머지(증권사 리서치 추천주, 정책 클러스터 재부각 등)는
데이터 소스 자체가 없어 이번엔 손 못 댐(아래 "다음 단계" 참조).

**수정**: `_DART_AGM_KW=("임시주주총회소집",)`으로 임시주총만 별도 식별(정기주총은 계속 스킵).
신규 `_agm_agenda_check()` — DART 상세문서를 열어 사명변경·상호변경·사업목적·액면분할·무상증자·
유상증자·인적/물적분할·최대주주변경·경영권·자기주식 등 재료성 키워드가 실제로 있는지 확인 —
있으면만 "📋 [임시주총 안건 감지]" 관찰 알림 발송, 형식적 안건(감사위원 선임 등)뿐이면 조용히
무시(스팸 방지).

**검증**: `py_compile` 통과. mock 시나리오 4개 — ①임시주총+재료성 안건(사명변경) → 감지 발송
②임시주총+형식적 안건만 → 조용히 무시(발송 0) ③정기주주총회소집공고(회귀) → 계속 스킵됨
④기존 공급계약 호재 경로(회귀) → 정상 동작 유지. 전부 기대대로 통과.

**다음 단계(이번엔 미착수, 참고용)**: 같은 갭분석 리포트가 "증권사 추천주 발표", "정책 클러스터
재부각", "거래량/거래대금 이상징후 실시간 감지"도 개선 방향으로 짚었음. 증권사 리서치는 무료로
안정적으로 긁을 수 있는 공개 API가 마땅치 않아 보류, 정책 클러스터는 뉴스 키워드 기반이라 저녁
브리핑 프롬프트 튜닝으로 접근 가능, 거래량 이상징후는 기존 `check_vol_surge`(15:00~15:25)를
장 시작 전 시간대에도 적용하는 확장이 후보. 사용자가 원하면 다음에 이어서 진행.

### 7.34 아침 라인업 최종점검 신규 추가 — 저녁픽은 미국장 열리기 전 데이터였다는 지적 반영

**배경**: 사용자가 예전 "라인업 선정 기준 전면교체"(자금유입 상위 → 저녁뉴스 기반, PR #8) 결정에
이어 다시 강조: "미국 시장 확인해서 근거로 종목을 뽑아줘야지... 전날 국장상황/뉴스/거래량/거래대금/
전날 미국 시장 흐름/미국시장 강한섹터... 아침까지 뉴스를 확인해서 최종적으로 선별해야지." 코드를
다시 보니 실제로 구조적 허점이 있었음 — `check_evening_news`는 17~22시(KST)에 실행되는데, 미국장은
그보다 훨씬 늦게(23~24시경) 열려서 이 시간엔 **미국장이 아직 열리기도 전**이라 `compute_macro`가
읽는 나스닥/SOX 수치는 '선물 지표'일 뿐 실제 마감 결과가 아님. 저녁 브리핑만으로 라인업을 확정하면
미국장이 실제로 어떻게 마감했는지는 전혀 반영 못 한 채 다음날 아침을 맞는 구조였음 — 사용자 지적이
정확했음.

**구현**: 신규 `check_morning_lineup_finalize()` — 07:00~08:35(장전브리핑 08:40 이전)에 실행,
당일 1회. 이 시간엔 미국장이 실제로 마감(~06시경)돼 있어 `compute_macro`가 '진짜 마감 실측'을
반환함. 여기에 간밤 신규뉴스(`_rss_news(hours=14)`)와 라인업 종목별 최신 뉴스(`_stock_news_titles`)를
더해, 저녁에 고른 라인업을 다시 검증 — **새 종목을 추가하지 않고** 기존 라인업 안에서 '유지/제외'만
Gemini에게 판단시킴(간밤 악재·미국장 실측이 재료와 반대방향이면 제외). Gemini 키가 없거나 실패하면
`_news_grade`로 감지된 간밤 악재 종목만 기계적으로 제외하는 최소 안전장치로 폴백. 변경이 있을 때만
`save_auto_lineup()`으로 `manju_watchlist.json`을 갱신하고 텔레그램으로 "❌ 제외 / ✅ 최종 라인업"
요약 발송 — 변경 없으면 조용히 넘어감(스팸 방지). 저녁픽이 오늘 것으로 정상 세팅됐을 때만
(`auto_day==오늘`) 동작하고, 사용자가 수동으로 라인업을 지정했으면(`auto:false`) 건드리지 않음.

**검증**: `py_compile` 통과. mock 시나리오 5개 — ①Gemini가 1종목 제외 판단 → 라인업 갱신+텔레그램
발송 확인 ②Gemini가 전부 유지 판단 → 저장/발송 없음(변경없으면 조용히) ③Gemini 키 없음+간밤
악재종목 있음 → 최소 안전장치로 해당 종목만 기계적 제외 ④전날 저녁픽이 오늘 것으로 세팅 안 됨
(`auto_day` 불일치) → 완전 스킵 ⑤시간창(07:00~08:35) 밖 → 스킵. 전부 기대대로 통과.

### 7.33 서산·머큐리 NXT 시세조회 연속실패 경보 재발 — 1회성 판정 캐싱 → 누적증거 기반으로 근본수정

**배경**: 9/15 17:07에 사용자가 실제로 받은 경보 — "서산 시세 조회 54회 연속 실패", "머큐리 시세 조회
54회 연속 실패"(둘 다 NXT 애프터마켓 16:00~20:00 구간). 이 정확히 같은 증상이 이 세션 초반에
한 번 "고쳤었는데" 재발한 것 — 당시엔 사용자 로컬 폴더가 git에 아예 연결 안 돼 있어서 수정이
전혀 반영이 안 됐던 게 원인이었지만, 이번엔 git 연결·여러 번 pull까지 다 된 뒤에도 재발해서
근본 원인을 다시 파야 했음.

**원인**: 예전 수정은 `_nxt_tradable()`로 "이 종목이 NXT에서 거래되는지"를 **하루 1번만 판정해서
캐싱**하는 방식이었음(`state["holdings_nxt_ok"]`). 그런데 이 1회 판정이 하필 NXT 세션 시작
경계(장중~애프터 전환 구간)처럼 시세가 일시적으로 비거나 반대로 잔여 호가가 잠깐 잡히는 타이밍에
걸리면, 그 잘못된 판정이 캐싱돼서 **하루 종일 안 고쳐짐** — 서산·머큐리가 실제로 NXT 비거래(또는
거래량이 너무 없어 호가가 안 잡히는) 종목인데 그 1회 판정 시점에 "거래됨(True)"으로 잘못 캐싱되면,
그 이후 진짜(정상적인) "시세 없음"이 전부 실패로 카운트되어 계속 경보가 쌓이는 구조였음.

**수정**: 1회성 판정 방식을 버리고, **"오늘 NXT에서 이 종목의 유효 시세를 실제로 한 번이라도
본 적 있는가"**라는 누적 증거 기반으로 교체(`state["holdings_nxt_seen"]`, 예전 `holdings_nxt_ok`
대체). 오늘 NXT에서 단 한 번도 시세를 못 봤다면 실패로 안 세고(비거래 종목일 가능성이 높은 정상
상태), 반대로 **이미 한 번이라도 정상 시세를 봤던 종목이 갑자기 조회가 안 되면** 그건 진짜 장애일
확률이 높으니 여전히 그대로 카운트해서 경보를 보냄 — 1회 판정에 의존하지 않고 계속 자기교정됨.

**검증**: `py_compile` 통과. mock으로 3개 시나리오 확인 — ①서산·머큐리처럼 NXT에서 하루종일 단
한번도 유효가가 안 나오는 종목을 60틱 연속 시뮬레이션 → 경보 0건·실패카운터 누적 없음(버그 재현
안 됨 확인) ②정규장에서 진짜 API 장애 → 여전히 정상적으로 경보 발송(회귀 없음) ③NXT에서 처음
2번은 정상 시세가 나왔다가(=실제 거래되는 종목임을 증명) 갑자기 계속 끊기는 경우 → 여전히 정상
경보 발송(진짜 장애 탐지 능력 유지).

### 7.32 "왜 신호가 안 오는지" 진단 로그를 9강 신호 전체로 확대

**배경**: 사용자가 종배픽/시간외단일가/시가배팅/돌파매매/눌림매매/짝꿍매매/상따매매/단기스윙 9개
탭으로 정리한 스프레드시트를 보여주며 "이거는 왜 안주는거야?? 뭐가 문제야? 왜 메세지가 안오는지
확인해봐"라고 강하게 요청. 이 세션(사용자 PC/텔레그램 로그가 없는 원격 세션)에서는 실제 그날
메시지가 안 온 이유를 로그로 직접 볼 수 없어서, 코드 레벨로 8개 함수(눌림·돌파·짝꿍·상따·단기스윙
+ 이미 진단로그 있던 시가배팅)를 전부 다시 읽고 확인.

**확인 결과**: 8개 함수 전부 메인 루프에 정상적으로 연결돼 있고 로직 자체에 구조적 버그는 없음.
다만 짝꿍매매(대장주 +15%↑ 필요)·상따매매(종목 자체가 +25%↑ 필요)는 원래 극단적인 급등이 그날
있어야만 발동하도록 설계된 관찰용 신호라(강의 원작 그대로, API로 실시간 호가/VI를 못 봐서 근사),
그런 날이 없으면 0건이 뜨는 게 정상 동작임. 문제는 그동안 0건일 때 **아무 로그도 안 남아서**
"감시가 안 돌았다"와 "돌았는데 조건에 안 맞았다"를 구분할 방법이 없었던 것(9/10 시가배팅에서
이미 한 번 발견해 고쳤던 것과 동일한 종류의 사각지대).

**수정**: `check_pullback_scan`(눌림)·`check_breakout`(돌파)·`check_limitup_follow`(상따)·
`check_pair_trade`(짝꿍)에 시가배팅과 동일한 패턴의 "0건 스캔완료" 진단 로그 추가 — 몇 종목이
1차 후보에 올랐고 그중 몇 건 발송했는지, 후보 자체가 없었는지/후속 게이트에서 걸렸는지를 매
스캔 사이클마다 콘솔에 남김(`check_swing_scan`은 기존에 이미 있었음). 코드/신호 로직 자체는
변경하지 않음(오탐 줄이려고 일부러 좁게 만든 게이트를 건드리면 승률이 떨어질 위험) — 오직
"왜 안 왔는지"를 나중에 콘솔 로그만 보고도 알 수 있게 가시성만 추가.

**검증**: `py_compile` 통과. 4개 함수 각각 후보 0종 상황을 mock으로 만들어 "발송 0건" 진단 로그가
정확히 찍히는지 확인.

**사용자 액션**: 다음에 "왜 신호가 안 왔지" 싶으면, watcher 콘솔/로그 파일에서 해당 시간대의
`[눌림타점]`/`[돌파매매]`/`[상따관찰]`/`[짝꿍관찰]` 줄을 찾아서 캡처해 보내주면 "후보 자체가
없었는지" vs "후보는 있었는데 게이트에서 걸렸는지"를 바로 알 수 있음.

### 7.31 DART 공시 알림 — "전부 관망"이라 재료 품질이 안 보이던 문제 수정

**배경**: 사용자가 실제 수신한 예시 — 기가비스(420770) 단일판매·공급계약체결(+9.8% 상승중)이
"📢 [공시·리스크오프 관망]"으로만 옴. 사용자 피드백: "모든 공시가 다 관망이야.. 어떤 공시가
좋은공시인지 한눈에 보여줬으면 좋겠어." 코드 확인 결과 `check_dart_disclosures`는 호재 공시라도
①거래대금부족 ②과열 ③리스크오프(sev2) ④하락중 ⑤임팩트약함 중 하나라도 걸리면 전부 "관망"
계열 메시지로만 나가고, 정작 '이 재료 자체가 강한지 약한지'는 맨 마지막(임팩트 판정) 단계까지
가야만 계산돼서 그 전 분기(과열·리스크오프 등)로 빠지면 전혀 안 보였음 — "타이밍 문제라 관망"과
"재료 자체가 약해서 관망"이 똑같은 문구로 나가 사용자가 구분 불가능했던 게 근본 원인.

**수정**: 신규 헬퍼 `_dart_material_grade()` — 가격조건 분기 전에 재료 품질을 먼저 한 번만
계산해서 🔥강한재료/🟢보통재료/🌱약한재료 라벨을 부여(공급계약·단일판매·수주=매출대비%로 정량 판정,
기존 임팩트 로직 재사용 / 기술이전·특허·임상 등 금액 파싱 불가한 유형=뉴스등급으로 보조판정).
이 라벨을 관찰·선점/과열/리스크오프/하락중/임팩트약함/최종 진입후보까지 **모든 메시지 제목에
동일하게 부착** — 이제 "🔥강한재료·리스크오프 관망"처럼 재료 품질과 보류 사유가 분리되어 보임.

**검증**: `py_compile` 통과. mock으로 5개 시나리오 검증 — ①강한재료+리스크오프(🔥강한재료 라벨+
리스크오프관망 문구 동시 확인) ②약한재료+정상장세(🌱약한재료+임팩트약함관망) ③강한재료+전조건
통과(진입후보 메시지에도 라벨 유지) ④비계약형(기술이전)+뉴스등급S+장외(뉴스등급 기반 라벨 정상)
⑤악재공시 회귀확인(라벨 로직이 악재 분기엔 영향 없음, 기존 메시지 그대로) — 전부 기대대로 통과.

**"속도" 관련(별도 확인, 코드 변경 안 함)**: 감시 루프는 이미 정규장(08:00~15:22)·NXT(16:00~19:50)엔
60초 주기로 돌고 있고, DART 조회는 그 사이클 초반부에 실행됨 — 폴링 주기 자체는 크게 느리지 않음.
다만 전체 사이클이 ~40개 체크함수를 순차 실행하다 보니 일부 시간대엔 실제 주기가 60초보다 늘어질
수 있고, 무엇보다 DART 공식 공시 접수시각과 시장이 반응하는 시각 사이엔 원래도 갭이 있을 수
있음(공시 전 정보가 이미 일부 반영돼 있는 경우). DART만 더 빠르게(예: 20초 간격 전용 스레드)
폴링하는 건 가능하지만 `state`/텔레그램 발송을 다른 체크함수들과 동시에 건드리게 돼서 스레드
안전성 검토가 필요한 더 큰 변경이라 이번엔 보류 — 사용자가 원하면 별도로 설계·구현.

### 7.30 RSI·CMF·MACD 3중 확인 반등 검색기 신규 추가

**배경**: 사용자 요청 — "RSI 30 이하 눌림 반등 신호 - OBV 상승 신호 - MACD 신호 -10/-5/0 신호
순차적으로 잡히는 검색기 만들어줘 · 텔레그램 발송 · 거래대금/거래량도 중요". 사전 확인(2문항):
①OBV 신규구현 vs indicators.py의 CMF 재사용 → **CMF 재사용 확정**(indicators.py에 이미
"V8.9.2 OBV 폐기→CMF 교체" 기록 존재, 재구현 금지 원칙에 부합). ②3신호 AND(전부 동시충족) vs
각각 독립발송 → **AND(전부 동시충족) 확정**.

**구현**: `_daily_ohlcv()`(신규 헬퍼 — `_daily_setup`과 동일 TR/inquire-daily-price 재사용,
최신순 응답을 과거→최근순으로 뒤집어 지표계산용 시계열 반환) + `check_rsi_cmf_macd_recovery()`
(신규, 09:05~15:20·거래대금 상위 유니버스). indicators.py의 `calc_rsi_wilder`/`calc_macd`/
`calc_cmf`를 그대로 import해 사용(신규 재구현 안 함 — 재사용 원칙 준수). 3조건:
①RSI 최근5일 중 과매도(≤30) 이력 + 오늘 반등전환(전일↑) + 과열前(≤50)
②CMF20 3일 연속 상승(매집 전환, OBV 대체)
③MACD 직전10일 저점 ≤-10 → 이후 -5 이상 통과 이력 → 현재 -2~+5 구간에서 상승중(0 회복)
— 3개 전부 충족해야 발송. 게이트: 거래대금 30억↑, 거래량 5일평균 대비 1배↑(회복 확인),
극단적 급락/급등(-8%~+5% 범위 밖) 제외, 악재 종목(_news_grade) 제외, my_watch 중복 제외,
종목별 하루 1회(일봉 기반 신호라 시간쿨다운 대신 당일 게이트).

**주의(설계상 가정)**: "MACD -10/-5/0"은 사용자가 명시한 그대로의 절대값(원 단위)을 기준으로
구현 — MACD는 주가 스케일에 종속적이라 이 임계값은 저가~중가 종목대에 맞을 가능성이 높고,
초고가주(수십만원대)에서는 잘 안 맞을 수 있음. 실전 발송 로그 보고 필요시 임계값 조정.

**검증**: `py_compile` 통과. 지표 계산 자체는 monkeypatch(indicators.calc_rsi_wilder 등 직접
대체)로 로직만 분리 검증 — ①3조건 전부충족→발송 ②RSI만 위반→미발송 ③CMF만 위반→미발송
④MACD 저점 미달→미발송 ⑤저점 직후 급반등(중간값 없이 바로 0권 진입)도 허용되는지(순서 로직
정확성) 5개 시나리오 모두 기대대로 통과. 추가로 게이트 단위 검증: 거래대금<30억(사전컷,
ohlcv API 호출 자체 안 함) / 급락-12%(사전컷) / 거래량0.5배(사전컷) / 당일 중복호출시 1건만
발송(하루 1회 게이트) / sev=2(리스크오프) 완전스킵 / 장시간외(08:30) 스킵 — 전부 확인.

### 7.3 실전투자 안정성 점검(이번 세션) — "알림이 없다=안전하다"를 깨는 침묵 실패 제거

사용자 요청으로 "실전투자 시 문제될 사항"을 코드 레벨에서 선제 점검·수정:

1. **JSON 저장 비원자적 쓰기** — `save_state`/`signal_scorecard`/`pick_history` 등 8곳 모두
   쓰는 도중 프로세스가 죽으면(정전·강제종료) 파일이 깨지고, 읽는 쪽 `except`가 조용히 `{}`로
   리셋해 당일 중복알림·성적기록 유실을 알아챌 방법이 없었음.
   → `_atomic_write_json`/`_atomic_write_text`(임시파일 쓰기 후 `os.replace`)로 전부 교체.
2. **보유종목 시세 조회 실패 시 침묵** — `check_holdings`가 시세 조회 실패 시 그냥 다음 종목으로
   넘어가서, KIS 장애가 지속되면 손절/익절 감시가 "알림 없이" 계속 멈춰있어도 사용자는
   "조용하니 괜찮다"고 착각할 위험. → 종목별 연속 3회 실패 시 "감시 중단 중" 경보 1회(쿨다운 60분).
3. **my_holdings.json 파싱 실패도 침묵** — 메모장으로 손절값 고치다 JSON 문법이 깨지면
   `_read_holdings()`가 `[]`를 반환해 전체 보유종목 감시가 티 안 나게 꺼짐.
   → `_holdings_health_check()` 신설, 장 시작 무렵(08:30~09:30) 하루 1회 파싱 검증,
   실패 시에만 경보(정상적인 `on:false`는 경보 안 함).
4. **텔레그램 전송 실패 시 재시도 없음** — 유일한 알림 채널인데 순간 네트워크 끊김·텔레그램
   서버 일시 5xx 한 번이면 손절 알림 같은 중요 메시지가 그냥 유실됐음. → 청크별 최대 3회
   재시도(2초 간격)로 `send_telegram` 강화.
5. **점검했지만 문제없었던 것들**: 모든 `requests.get/post` 호출에 `timeout=`이 이미 있어
   행(hang) 위험 없음. `.gitignore`에 `secrets.toml`·`.env`·런타임 데이터 파일 이미 제외돼있어
   API 키 커밋 유출 이력 없음(단, `tools_gui.py`에 키를 직접 하드코딩하는 구조라 사용자가
   `git add -A`로 실수 커밋할 구조적 위험은 남아있음 — 다음에 손대게 되면 `secrets.toml`
   읽어오는 방식으로 바꾸는 걸 권장).

### 7.4 저녁뉴스 재료 등급 정교화 (외부 리뷰 반영, 이번 세션)

실제 발송된 저녁뉴스 브리핑을 외부 리뷰(사용자 지인 AI)로 검토받은 결과, "사실은 맞지만 종목 연결을
과장"하는 패턴이 반복 지적됨(브렌트유 100달러↔흥구석유 실적 확정처럼 취급, ETF 편입을 A급 직접수혜로
분류, 오너家 특수관계인 장외 지분거래를 장내 매도압력처럼 서술 등). `check_evening_news` Gemini
프롬프트에 반영:
- 재료강도(상/중/하) → **직접성 등급 A/A-/B/C/D**로 교체(A=DART공시·확정계약, B=산업수혜·ETF편입·리포트,
  C=유가·환율·지정학 등 거시/테마, D=루머·재탕).
- 자주 과대평가되는 4개 패턴을 프롬프트에 명시적으로 박음: ①ETF 편입≠매출계약 ②특수관계인 장외거래≠
  장내 매도물량 ③해외기업 투자참여(개별 투자금·지분율·공급계약 비공개)≠확정 매출호재 ④유가 급등 하나로
  관련테마 전체를 강재료로 묶지 않기(거래대금·이격 확인 전엔 신규추격 근거 약함).
- "네이버 많이 본 뉴스"(이번 세션 앞서 추가한 개미 클릭 랭킹)를 🧠시장심리 문단에서만 참고하도록 명시
  제한 — 📌주목 테마(종목 선정)의 직접 근거로 쓰지 말라고 프롬프트에 못박음(원래 문구가 "주목테마 판단에
  우선 반영"이라고 돼있어 종목선정 근거로 새는 여지가 있었음).
- (보류) 외부 리뷰가 제안한 "0~24h/2~5일/6~30일/30일+ 시간대별 뉴스 버킷 분리"와 "마크다운 표 형식
  텔레그램 출력"은 미구현 — 전자는 RSS가 최근 기사만 주는 구조라 과거 며칠치를 쌓아 재조회하는 저장소가
  새로 필요한 큰 작업이고, 후자는 텔레그램 모바일에서 표 형식이 잘 안 읽혀 UX 후퇴라 판단.

### 7.5 실제 텔레그램 하루치 로그 분석 — 홀딩판정↔손절알림 모순 수정 (이번 세션)

사용자가 2026-09-09 하루 동안 실제로 받은 텔레그램 메시지 전체(장전~마감복기~저녁뉴스)를 공유해줘서
"뉴스→종목선정→실제 올랐나" 관점으로 전수 분석. 발견한 것 중 코드로 고칠 수 있는 실제 버그 1건 수정:

- **문제**: 08:02에 알테오젠에 대해 두 메시지가 동시에 나감 — "🌅 보유 홀딩 판정: 🟢 9시까지 보유"(재료A+수급유입
  근거)와 "💼 손절선 이탈: 정리 검토"(수익률 -5.2%, 손절기준 -2%)가 서로 모순. 원인: `_holding_judge()`가
  "밤사이 등락률"만 보고 판단해서 매수평균 대비 손익(이미 손절선 훨씬 이탈)을 전혀 몰랐음.
- **수정**: `_holding_judge(..., ret=, stop=)`로 매수평균 대비 수익률·손절기준을 받아, 이미 손절선 이탈
  상태면 재료 판단과 무관하게 결론을 "🔴 손절 원칙 우선(존버 금지)"으로 덮어쓰게 함(`check_holdings`
  8시 판정과 `_holdings_report`/`--holdings` 양쪽 다 반영). 사용자 룰(칼손절·존버 금지)이 재료 낙관론에
  밀리지 않도록.
- 그 외 분석에서 확인된 것(코드 수정 아님, 참고용):
  - 어제 넣은 ETF 편입 A급 방지 규칙이 저녁뉴스 19:35 브리핑에서 SK스퀘어·삼성전자에 다시 "등급A"로
    샜음 — 프롬프트 지시가 100% 지켜지진 않는다는 기존에 확인된 한계의 재확인. 반면 흥구석유(선반영으로
    강등)·홍라희 지분거래(장내매도와 분리 서술)는 규칙대로 잘 처리됨 — 완전 실패는 아니고 확률적 개선.
  - 그날 A급으로 3번 찍힌 알테오젠은 마감 +0.90%(라인업 최저)로 부진, 반대로 마감 최고(+9.64%)였던
    에코프로비엠은 하루 종일 A급/전조 알림이 한 번도 안 뜸 — "갭상승 원인 역분석"(매일 자동 실행)이
    이런 미스를 스스로 찾아내는 기능이 이미 있으므로, 그 누적 로그를 나중에 --analyze류로 정리하면
    "왜 놓쳤는지" 패턴을 더 볼 수 있을 것.

### 7.29 tools_gui.py 실제 키가 git pull마다 지워지던 문제 — 환경변수 우선 방식으로 수정

**배경**: git 미연결 상태였던 걸 7.x 앞부분에서 연결한 이후, 사용자가 tools_gui.py를 받을
때마다(git pull/checkout) 파일에 직접 써넣었던 실제 API 키가 GitHub의 placeholder
("PUT_YOUR_...")로 계속 덮어써져서 매번 다시 입력해야 하는 문제를 겪음. 홈/회사 모두
구글드라이브로 동일 폴더를 공유해 쓰고 있어 한 곳만 고치면 됨.

**원인**: `KEYS` 딕셔너리에 실제 키를 하드코딩하는 구조라, git이 관리하는 파일 자체에
비밀값이 들어있었음 — 파일이 갱신되는 한 구조적으로 계속 사라질 수밖에 없었음.

**수정**: `KEYS`의 각 값을 `os.environ.get(name) or placeholder`로 변경(`_key()` 헬퍼) —
**환경변수가 있으면 그걸 쓰고, 없으면 기존 placeholder로 폴백**. 실제 키는 이제 tools_gui.py
안에 두지 않고, 새로 만든 `run_gui.bat.example`(git 추적, placeholder만 있어 안전)을
`run_gui.bat`로 복사해서 `set KIS_APP_KEY=실제값` 식으로 채우게 함 — `*.bat`는 이미
.gitignore에 있어서 git이 앞으로 절대 이 파일을 건드리지 않음. 사용자는 이제 `tools_gui.py`
대신 `run_gui.bat`를 실행하면 됨(내부에서 set 후 `python tools_gui.py` 호출).

**검증**: `py_compile` 통과. `_key()` 함수를 직접 실행해 환경변수 있을 때/없을 때 각각
올바른 값(실제값/placeholder)을 반환하는지 확인.

**사용자 액션 필요**: `run_gui.bat.example`을 `run_gui.bat`로 복사 → 실제 키 9개 입력(기존에
tools_gui.py에 직접 넣었던 값 그대로 옮기면 됨) → 앞으로는 `run_gui.bat` 실행.

### 7.28 tools_gui.py에 관심종목 관리 창 추가 (보유종목과 동일 UX)

**배경**: 사용자가 "보유종목이랑 관심종목 같이 관리해도 되냐"고 물어서 — 신호 성격이 달라
(보유종목=손절/익절 감시용, 관심종목=신규진입 타점 탐색용) 분리 관리를 권했더니, "관심종목도
보유종목처럼 관리할 수 있게 만들어줘"로 요청 변경. 그동안 관심종목은 대시보드(클라우드)에서만
관리 가능했고 로컬 GUI(tools_gui.py)엔 보유종목 관리 창만 있었음.

**구현**: `_open_holdings_manager()`와 완전히 같은 UX(이름 검색→후보 선택→추가/삭제)로
`_open_watchlist_manager()`를 신규 작성 — 평단·수량 입력란만 없음(관심종목은 code/name만
필요). 코드 중복을 줄이려고 `_git_sync_holdings()`를 `_git_sync_file(filename, commit_msg)`로
일반화해 보유종목·관심종목 저장 함수가 공용으로 사용(둘 다 저장 직후 GitHub 자동 push).

**검증**: `py_compile` 통과. `_save_watchlist()`가 만드는 `{"on":true,"stocks":[{"code","name"}]}`
포맷을 실제로 파일에 써서 `mw._read_my_watch()`로 읽어보는 상호운용성 테스트 통과(macro_watcher가
읽는 스키마와 정확히 일치 확인). Tkinter GUI 자체는 이 sandbox에 디스플레이가 없어 창 렌더링은
직접 실행 못 함 — 사용자 환경에서 버튼 클릭 확인 필요.

### 7.27 "오늘 감시 라인업" 선정 기준 전면 교체 — 자금유입 순위 → 저녁뉴스 브리핑 기반

**사용자 문제제기**: "선별 기준은?"에 자금유입 상위(고정 15종목 풀) 방식이라고 답했더니 —
"미국 시장 확인해서 근거로 종목 뽑아야지, 전날 국장상황·뉴스·거래량·거래대금·미국시장흐름·미국
강한섹터 등등 확인해서 선정해줘야지"라고 반박. 조사해보니 **이미 그 모든 걸 하는 기능이 있었음**
(`check_evening_news`, 저녁 17~22시) — 미국장 실측+오늘 거래대금 상위 20종+종목별뉴스+Gemini
분석으로 "📌 주목 테마 TOP 3"를 뽑는데, 이 결과가 텍스트 리포트로만 발송되고 실제 "오늘 감시
라인업"(manju_watchlist.json)에는 전혀 반영이 안 되고 있었음 — 똑똑한 분석과 실제 감시 대상
선정이 완전히 분리돼 있던 구조적 문제.

**결정(사용자 확인)**: "완전히 교체" — 자금유입 상위(15종목 고정풀) 방식은 폐지하고, 저녁뉴스
브리핑이 라인업을 직접 결정하도록 변경. 단, 완전 삭제가 아니라 **최종수단 폴백으로 강등**
(gemini_key 미설정이거나 뉴스 수집 실패 등으로 저녁뉴스 쪽이 라인업을 못 정한 날에만 기존 자금유입
로직이 자동으로 작동).

**구현(재사용, 재구현 없음)**: `check_evening_news`엔 이미 "📌 주목 테마 TOP 3" 구간에서
`종목명(6자리코드)` 패턭으로 (이름,코드) 쌍을 뽑아 성적표에 기록하는 로직이 있었음(브리핑 픽
승률 추적용) — 그 정규식 추출 결과를 그대로 재사용해 `save_auto_lineup()`(자금유입 방식이
쓰던 기존 함수, `desc` 파라미터만 추가)에 넘겨 라인업을 갱신. 성공하면 `state["auto_day"]`를
**내일 날짜**로 찍어서, 기존 자금유입 폴백(장중 9~15시, 자기 날짜의 auto_day를 체크)이 그날은
이미 정해졌다고 보고 스스로 스킵하게 함 — 새 코드로 기존 폴백을 끄는 게 아니라, 기존 가드
조건을 그대로 이용해 자연스럽게 우선순위가 갈리도록 설계.

**검증**: 모킹 스크립트로 2가지 시나리오 확인 — ① Gemini 성공+종목코드 포함 리포트 → 라인업
갱신 + auto_day를 내일 날짜로 설정 ② Gemini 키 없음(실패) → 라인업 그대로(변경 없음), auto_day
미설정 → 자금유입 폴백이 정상적으로 작동할 수 있는 상태 유지. `py_compile`·`test_ai_briefing.py`
통과.

### 7.26 관심종목 대시보드→GitHub 자동반영 (보유종목과 대칭 조치)

**배경**: 사용자가 "라온텍이 왜 관심종목 알림에 계속 뜨냐"고 물어서 답하는 과정에 저장소의
`my_watch.json`에 라온텍·삼성전기가 아예 없는 걸 발견 — 대시보드(클라우드)의 🔍관심종목 탭에서
추가한 종목이 GitHub엔 반영 안 된 상태였음(7.22에서 "후속 조치 필요"로 남겨뒀던 바로 그 문제).

**원인**: 보유종목(7.22)은 "로컬(tools_gui.py)에서 쓴 파일을 어떻게 클라우드로 올리나"였는데,
관심종목은 정반대 방향 — **클라우드(대시보드)에서 쓴 파일을 어떻게 GitHub main에 올리나**였음.
클라우드 컨테이너엔 git 자체가 없거나 push 권한이 없어서 `tools_gui.py`처럼 `git push`
subprocess를 그대로 못 씀.

**수정**: macro_watcher.py에 이미 있던 `push_snapshot_github`(GitHub Contents API로 git 설치
없이 파일 업로드하는 패턴, `data` 브랜치의 snapshot.json용)를 참고해 quant_dashboard.py에
`_git_sync_watchlist()`를 신규 작성 — 다만 대상이 `main` 브랜치의 `my_watch.json`이라 별도
구현(기존 함수 재사용 아님, 패턴만 차용). `st.secrets["github"]["token"]`(GitHub PAT, repo
쓰기 권한)이 설정돼 있으면 관심종목 추가/삭제 직후 GitHub Contents API로 자동 커밋. 토큰이
없으면 크래시 없이 "이 배포판에만 남고 재배포시 사라질 수 있음" 캡션만 표시하고 기존처럼 계속
동작(회귀 없음).

**검증**: `py_compile` 통과, AppTest로 실제 관심종목 추가 폼 제출까지 실행 — 토큰 없는 이
sandbox 환경에서도 예외 0건 확인. (AppTest가 실제 my_watch.json에 테스트 종목을 써버려서
`git checkout`으로 원복함 — 실제 파일에 영향받는 통합테스트라 다음에도 주의.)

**사용자 액션 필요**: Streamlit Cloud 앱 설정 → Secrets에 아래 추가해야 실제로 작동:
```toml
[github]
token = "ghp_..."   # repo 쓰기 권한 있는 Personal Access Token
```

### 7.25 9/11 하루치 텔레그램 로그 실사용 검토 — 라벨 오표기 수정 + 재발 이슈 확인 필요

**사용자가 9/11 하루 전체 텔레그램 로그를 붙여넣고 "면밀히 검토해서 수정해야되는게 있는지" 요청.**

**실제 코드 버그 발견·수정**: 09:24 "🔄 오늘의 감시 라인업 자동 편입" 메시지가 종목 5개만
나열했는데 "(자금유입 상위 6종...)"으로 항상 고정 표기 — `auto_lineup_from_secs(secs, 6)`은
최대 6개를 요청하지만 `len(_auto) >= 3`이면 그보다 적어도(5개든 4개든) 진행하는데, 메시지의
개수 라벨은 실제 개수(`len(_auto)`)가 아니라 요청 파라미터 `6`을 하드코딩해서 찍고 있었음.
`len(_auto)`로 교체.

**검증했으나 이번엔 코드 버그 아님으로 판정한 것**:
- `check_my_watch`의 라온텍·LG에너지솔루션 등 "눌림 반등" 알림이 하루 2~3회 반복 발송된 것처럼
  보였는데, 실제 시각 간격(9:03→11:04→13:40 등)을 계산해보니 전부 120분 쿨다운을 넘겨서 발생한
  정상 재발동 — 중복 발송 버그 아님.
- 08:40 장전 브리핑 이후 다수의 "🌅 장전 예열" 갭다운 경보(11건)는 보유종목이 아닌 관심종목·
  라인업 전체 유니버스를 스캔하는 게 설계 의도 — 스팸이 아니라 그날 실제로 다수 종목이 동시에
  갭다운(반도체·2차전지 약세장)했던 것.

**⚠️ 재확인이 필요한 항목(코드는 이미 고쳐졌는데 재발한 것처럼 보임)**: 7.21에서 고친 "NXT
미거래 종목 애프터마켓 오탐 경보"(서산·머큐리 "시세 조회 N회 연속 실패")가 **9/11 로그에도
그대로 나타남**(08:03 "106회", 16:04 "3회", 17:05 "54회"). 코드 자체(`check_holdings`의
`_nxt_ok` 캐싱)는 현재 저장소에 정상 반영돼 있음을 재확인했고, 카운터가 이날 09:01 정규장
성공으로 리셋됐다가 애프터마켓 구간에서 다시 0→54로 선형 증가한 패턴은 **수정 전 로직 그대로
동작한 것과 정확히 일치** — 가장 유력한 원인은 **사용자 PC에서 도는 감시(watcher) 프로세스가
7.21 수정 이후 재시작(또는 `git pull` 자체)이 안 됐다는 것**(파이썬 장기 실행 프로세스는 파일이
바뀌어도 재시작 전엔 메모리의 구코드를 계속 씀 — 이 세션에서 이미 한 번 겪은 패턴). 사용자에게
`git pull` 후 감시 재시작을 요청함 — 재시작 후에도 재발하면 그때는 진짜 코드 결함(예: 이 종목들이
실제로는 NXT 거래가 되어 가끔 시세가 잡히는 사례)일 가능성까지 열어두고 재조사 필요.

### 7.24 check_opening_bet "0건 침묵" 진단 로그 추가 (사용자 문의로 발견)

**배경**: 사용자가 9개 전략(시가배팅·돌파매매 등) 스크린샷을 보여주며 "이거 다 셋팅해놨는데 왜
신호가 없냐"고 문의. 9개 전략 전부 `main()` 루프에 실제로 연결돼 있음을 확인했고(죽은 코드
없음), 9/10 09:00~09:10 시가배팅 조건 통과 종목이 있었는지 사용자가 구체적으로 물어봄.

**사각지대 발견**: 이 세션 sandbox엔 KIS 키·사용자 실제 로그(signal_scorecard.json 등, 전부
gitignore 대상)가 없어 직접 조회 불가 — 사용자에게 콘솔 로그를 요청했는데, `check_opening_bet`은
**조건 맞는 종목이 0건이어도 아무 로그를 안 남겨서**, "감시가 꺼져있었다"와 "감시는 됐는데
후보가 없었다"를 로그만으로 구분할 방법이 없었음(`check_swing_scan`은 "[단기스윙] 후보 0종"을
이미 찍고 있어 비교로 발견).

**수정**: 스캔 루프 종료 후 발송 건수(`_sent_n`)가 0이면 진단 로그 1줄 출력 —
`[시가배팅] HH:MM 스캔 완료 — 갭 조건 통과 후보 N종 중 발송 0건(후보 자체가 없음 / 재료·수급·
프로그램 등 후속 게이트 미충족)`. 갭 조건(등락률+거래대금)까지만 통과한 1차 후보 수(`_budget`
재사용)를 같이 찍어서, "애초에 후보가 없었다"와 "후보는 있었는데 재료/수급 게이트에서
떨어졌다"를 구분 가능.

**검증**: 모킹 스크립트로 3케이스 확인 — ①갭조건 후보 0종(후보 자체가 없음), ②갭조건 통과
1종이 재료미확인으로 탈락(후속 게이트 미충족), ③실제 발송 성공 시엔 진단 로그 안 찍힘(회귀 없음).

**참고— 9개 전략 신호 부재 원인 정리(향후 유사 문의 대응용)**:
- 짝궁매매·상따매매(7·8강)는 애초에 "관찰용" 알림이지 매수신호가 아님(코드 주석에 명시).
- 돌파매매·눌림매매·짝궁·상따·단기스윙은 `sev==2`(리스크오프)면 함수 진입부에서 즉시 return —
  매크로 킬스위치가 걸리면 통째로 침묵하는 게 설계된 안전장치.
- 시가배팅(09:00~09:10)·단기스윙(15:00~15:25)·시간외단일가(17:00~18:30)는 창이 매우 좁아서,
  그 시간에 감시가 안 돌았거나 조건 맞는 종목이 그날 없었으면 조용한 게 정상.
- "종가배팅"은 종배픽(`check_dolpanty_pick`)의 원래 이름 — 별도 기능이 아니라 이미 활발히
  동작 중인 그 기능임을 이번에 확인.

### 7.23 대시보드 UI 개선 — 모바일 반응형 + 정보 우선순위 재배치

**사용자 피드백**: "전체적인 디자인/가독성", "모바일에서 보기 불편함", "정보 배치/우선순위",
"특정 탭/화면" 4개 전부 개선 요청.

**실제 헤드리스 브라우저(Playwright+Chromium)로 데스크톱(1280px)·모바일(375px) 스크린샷을
찍어가며 검증** — 코드만 보고 추측하지 않음. 첫 모바일 스크린샷에서 진짜 문제 발견:
`initial_sidebar_state="expanded"`가 좁은 화면에서도 사이드바를 강제로 펼쳐서 **본문을
화면 밖으로 밀어내는** 심각한 버그였음(글자가 잘려서 겹쳐 보임). `"auto"`로 바꿔서 좁은
화면에서는 자동으로 접히도록 수정 — 이게 가장 큰 임팩트였음.

**추가 수정**:
- 전역 CSS로 680px 이하에서 `st.columns`를 가로 압착 대신 세로 스택으로 전환
  (`[data-testid="stHorizontalBlock"] { flex-direction: column }`) — 보유종목 카드,
  관심종목 행, 지표 3분할 등 컬럼 쓰는 모든 화면에 일괄 적용됨. 모바일 스크린샷으로
  관심종목 탭이 실제로 세로로 잘 쌓이는 것 확인.
- 탭 라벨 폰트 키움(터치 타깃 개선), 지표 라벨 굵게.
- 🏠오늘현황: AI 브리핑을 신호등 색상 좌측 테두리 카드로 강조(결론 문장이 가장 크게),
  상세 3줄은 기본 접힌 expander로 이동(정보 우선순위 — 결론부터, 근거는 펼쳐서).
  오늘 신호 타임라인도 expander로(신호 없는 날엔 접혀서 화면 차지 안 함).
- 보유종목 요약 카드: 손절선 이탈 종목엔 종목명 앞에 🔴 표시(눈에 먼저 띄게).
- 보유종목 탭 각 행에 "현재가"/"수익률·평가손익"/"판정" 캡션 라벨 추가 — 모바일에서
  컬럼이 세로로 쌓였을 때 숫자만 덩그러니 있지 않고 뭘 보는 값인지 알 수 있게.

**검증**: `test_ai_briefing.py` 통과, AppTest 예외 0건, 데스크톱은 기존 레이아웃 그대로
유지되는지(미디어쿼리라 680px 초과에서는 무영향) 스크린샷으로 재확인.

### 7.22 tools_gui.py 보유종목 저장 → GitHub 자동 push (클라우드 대시보드 미반영 문제 해결)

**증상**: 사용자가 tools_gui.py "보유종목 관리" 창에서 보유종목을 갱신했는데(알테오젠·서산·머큐리로
변경) 대시보드엔 반영이 안 됨.

**원인**: 대시보드는 Streamlit Cloud(클라우드)에서 돌고, tools_gui.py·macro_watcher.py는
사용자 PC(로컬)에서 돈다 — **서로 다른 컴퓨터**. `my_holdings.json`은 git으로 커밋된 파일을
Streamlit Cloud가 clone해서 쓰기 때문에, 로컬에서 GUI로 파일만 바꾸고 git commit+push를
안 하면 클라우드 쪽은 예전 내용을 계속 보여준다. 실제로 저장소의 my_holdings.json 마지막
커밋은 "알테오젠·원익·서산"으로, 사용자 로컬의 최신 상태(알테오젠·서산·머큐리)와 달랐음 —
"파일 공유 재사용" 설계가 로컬-로컬 구성(macro_watcher↔tools_gui)에서는 맞지만
로컬-클라우드 구성(tools_gui↔대시보드)에서는 깨진다는 걸 이번에 실사용으로 확인.

**수정**: `tools_gui.py`의 `_save_holdings()`가 파일 저장 직후 `_git_sync_holdings()`를
호출 — 백그라운드 스레드에서 `git add my_holdings.json && git commit && git push`를
자동 실행(GUI 멈춤 없음). 성공/실패를 실행 로그창에 표시. 변경 없음(nothing to commit)이면
조용히 종료. 네트워크 실패 시엔 "수동으로 git push 해주세요" 경고만 띄우고 앱은 정상 동작.

**미해결로 남긴 것**: 대시보드의 🔍관심종목 탭은 반대 방향 문제가 있음 — 클라우드에서 직접
`my_watch.json`을 쓰는데, 이건 git에 반영되지 않아 **다음 배포(redeploy)때 사라질 수 있는
임시 변경**이고 로컬 macro_watcher.py에도 전달 안 됨. 사용자가 관심종목을 대시보드에서
자주 바꾼다면 이것도 같은 방식(대시보드→GitHub API로 커밋, 또는 로컬 전용으로 用途 제한)의
후속 조치가 필요 — 이번 세션에서는 보유종목만 요청받아 우선 처리.

### 7.21 실제 텔레그램 로그로 발견 — NXT 미거래 종목 애프터마켓 오탐 경보 수정

**증상**: 실제 운영 중 "서산 시세 조회 54회 연속 실패", 1시간 뒤 "105회 연속 실패" 경보가
반복 발송됨(머큐리도 동일). 사용자가 "손절/익절 자동감시가 안 되고 있다"는 경보를 보고 문의.

**원인**: `check_holdings()`는 16:00~20:00(애프터마켓)·08:00~08:50(프리마켓) 구간엔 무조건
`mrkt="NX"`(넥스트레이드)로 시세를 조회한다. 그런데 **NXT에서 아예 거래되지 않는 종목**은
이 시간대에 시세 자체가 존재하지 않아 `_price_and_turnover`가 항상 `(None,None,None)`을
반환하는 게 정상이다 — 이걸 코드가 "API 장애로 인한 조회 실패"와 구분하지 않고 그냥 실패
카운터를 계속 올려서, 실제로는 아무 문제 없는데 매시간(쿨다운 60분) 오탐 경보가 나갔다.

**수정**: 실패로 카운트하기 전에 `_nxt_tradable()`(기존 함수, NX 시세 유무로 판별)을 종목당
하루 1회만 호출해 `state["holdings_nxt_ok"]`에 캐싱 — NXT 비거래로 확인되면 그 종목은 이
시간대에 조용히 스킵(카운트도 경보도 없음). 정규장(mrkt="J") 중 진짜 API 실패는 기존 로직
그대로 3회 이상 실패 시 경보(회귀 없음, 별도 시뮬레이션으로 확인).

**검증**: 실제 네트워크 없이 `_price_and_turnover`/`_nxt_tradable`/`send_telegram`을 모킹한
스크립트로 2가지 시나리오 확인 — ① NXT 비거래 종목 5회 연속 호출 → 경보 0건(수정 전이면
3회째 발송됐어야 함) ② 정규장 진짜 실패 5회 연속 호출 → 기존과 동일하게 3회째 경보 발송.



**배경**: 실제 하루치 텔레그램 로그 분석 중 사용자가 "대시보드를 macro_watcher의 최신 버전으로
업데이트하자, 완전히 처음부터 재구축"을 요청. 조사 결과 기존 대시보드는 macro_watcher.py와
**완전히 분리되어 독자 진화**한 21,025줄짜리 코드였음 — 특히 "종배" 스코어링을 대시보드가 자체
재구현(`_dolpanty_score`/`scan_tomorrow_candidates`)하고 있어 텔레그램 실제 판단과 대시보드가
보여주는 판단이 서로 다를 수 있는 구조적 문제가 있었음. 또한 상당 부분(만쥬 스캘프 모니터,
페이퍼 트레이딩, 연기금 추적, Night/Day Strike 등 ~3000줄, `generate_ai_briefing`)이 죽은 코드거나
기능 플래그(`_SHOW_PENSION/_SHOW_SWING/_SHOW_REALTRADE`)로 꺼져서 실제로는 안 보이는 상태였음.

**핵심 설계 원칙 — "재구현 금지, 재사용"**: 새 대시보드는 `import macro_watcher as mw`로
macro_watcher.py를 그대로 불러와서 실시간 판단 함수(`compute_macro`·`_holding_judge`·
`_regime_detect`·`_volatility_scan`·`_price_and_turnover`·`read_kis_keys`·`kis_token` 등)를
직접 호출한다. 집계 데이터(신호별 성적)는 macro_watcher가 이미 쓰는 파일
(`signal_scorecard.json`·`pick_history.json`)을 pandas로 읽어 화면용으로만 재현(순수 집계라
로직 드리프트 위험 낮음). `market_review.md`는 `mw._mr_read_all()`로, 보유종목/관심종목은
`my_holdings.json`/`my_watch.json`을 **macro_watcher와 동일 파일 그대로** 공유. 이 구조 덕분에
앞으로 macro_watcher.py를 고치면 대시보드도 자동으로 최신 로직을 반영 — 오늘 겪었던
"코드는 고쳤는데 실제 동작은 예전 버전"류 드리프트가 이 축에서는 구조적으로 사라짐.

**사용자 결정사항 반영**:
- 가상 페이퍼트레이딩(Firebase 가상계좌·매매내역, 실신호와 무관) → 삭제
- KIS 실주문 코드(`REAL_TRADING_ENABLED=False`) → macro_watcher는 처음부터 신호전용(자동매매 없음)
  원칙이라 대시보드도 일관되게 실주문 UI 자체를 만들지 않음(저수준 함수도 새 대시보드엔 없음 —
  필요해지면 그때 별도로 이식)
- 로그인(Firebase 멀티유저 인증) → 그대로 포팅
- 관심종목 Google Sheets 이중저장 → 제거, `my_watch.json` 직접 CRUD로 대체

**새 구성 (8탭)**: 🏠오늘현황(매크로배지·보유요약·오늘신호타임라인) · 📊성과분석(신호별
승률/평균수익, `_analyze_history`와 동일 알고리즘) · 🌙시장복기(`market_review.md` 카드뷰) ·
💼보유종목(`_holding_judge` 실시간 배지) · 🎯종배픽·재료등급(`_regime_detect` + 종배 기록) ·
🔍관심종목(`my_watch.json` CRUD) · 🔎라이브스캐너(`_volatility_scan` 그대로 호출) ·
⚙️설정·진단(키 연결 상태·감시 프로세스 최근 갱신시각).

**검증**: `python -m py_compile` 통과, `streamlit.testing.v1.AppTest`로 헤드리스 실행 —
예외 0건, 8개 탭 전부 정상 렌더, 시크릿 없는 이 sandbox에서도 "KIS 키 없음" 등 폴백 문구가
깨지지 않고 나옴. 실제로 `my_holdings.json`(3종)·`my_watch.json`(6종)을 읽어 정확한 개수를
표시하는 것까지 확인 — macro_watcher와의 파일 공유가 실제로 작동함.

**requirements.txt 정리**: pykrx·finance-datareader·cloudscraper·beautifulsoup4·lxml·gspread·
google-auth·aiohttp·nest_asyncio 제거(더 이상 안 씀), streamlit(원래 누락돼 있었음)·google-genai 추가.

**미확인 사항(사용자 환경에서 확인 필요)**: 이 sandbox엔 KIS/Firebase/Gemini 실키가 없어 실제
로그인·실시간 시세·매크로 조회까지는 검증 불가 — 시크릿 있는 실제 환경에서 로그인부터 각 탭
데이터가 실제로 잘 뜨는지 최종 확인 필요.

### 7.19 성적표 이상현상 추적 — KIS rt_cd 미검증 발견(코드베이스 전체 패턴)

7.18에서 추가한 진단 로그로 재현 확인 — 같은 5종목(삼성전기·중앙에너비스·한화오션·대한광통신·
우리기술)에서 매번 동일하게 "현재가==추천가"인데 고/저는 실제로 크게 다른 모순 발생. `_price_full()`
코드를 다시 보니 **`rt_cd`(KIS 응답 성공/실패 코드)를 전혀 검증 안 하고 "output이 비어있지 않으면
성공"으로만 판정**하고 있었음 — `grep -n "rt_cd"`로 전체 파일을 확인한 결과, 이 코드베이스에서
`rt_cd`를 실제로 검사하는 곳이 **단 한 곳도 없음**. 레이트리밋 등으로 `rt_cd!=0`인데 `output`
필드에 stale/기본값이 들어있는 응답을 그대로 신뢰했을 가능성이 있음(11개 종목을 딜레이 없이
연달아 조회하는 `_scorecard_report`에서 특히 재현되기 쉬운 패턴).
- **수정**: `_price_full()`에 `rt_cd` 검증 추가 — `"0"`(성공) 또는 빈 값이 아니면 실패로 간주해
  `(None,...)` 반환 + `[시세조회 진단]` 로그로 `rt_cd`·`msg1`·`output` 스냅샷을 출력.
  `_price_full()`의 20여개 호출부는 이미 전부 `if not px`/`if _o:` 같은 falsy 체크로 None을
  안전하게 처리하고 있어 회귀 위험 낮음(직접 확인함).
- **아직 확정 아님**: 이게 진짜 원인인지는 다음 실행에서 `[시세조회 진단]` 로그가 그 5종목에 대해
  뜨는지 봐야 확정됨 — 뜨면 원인 확정(레이트리밋/degraded response), 여전히 "현재가==추천가"
  모순이 rt_cd 진단 없이 재현되면 다른 원인(다른 필드 이슈 등)으로 계속 추적 필요.
- **다른 KIS 호출부 전체로 rt_cd 검증을 확대할지는 보류** — `_price_full` 한 곳에서 가설 검증 먼저
  하고, 확정되면 범위를 넓히는 게 안전(20여개 함수를 한 번에 바꾸는 건 리스크 큼).

### 7.18 실제 성적표 결과 분석 — 과열주 하드태그 추가 + 성적표 자체 버그 2건 발견

사용자가 공유한 실제 `📋성적표` 결과(브리핑 11종·종가승률 55%·익절도달 73%)를 분석. 6차 외부
리뷰도 같은 데이터를 보고 "손절 -2% 규칙인데 우리기술·대한광통신은 -16.6%/-5.0%까지 밀렸다"고
지적 — 이건 **이 시스템에 자동매매 체결 기능이 없다는 걸 오해한 것**(텔레그램 알림의 손절가는
'참고 가격'이지 자동 매도 주문이 아님. `✂️손절이탈` 태그도 "장중 -2% 터치"일 뿐 "매도 체결됐다"는
뜻이 아니고 원래 문구도 그렇게 표기돼 있음). 다만 **우리기술이 전일+21.2%·이격+22%·재료불명인데도
추천에 포함돼 실제로 -16.6%까지 밀린 건 진짜 문제** — 세션 초반부터 여러 차례 "재료 직접성 낮음"
경고를 했는데도 프롬프트 지시만으론 Gemini가 계속 놓침. 재료등급 판단은 LLM 재량이라 신뢰도가
낮으니, 이미 계산돼있는 전일 등락률에 **하드 태그**를 박아 판단 부담을 줄임: `[오늘 거래대금 상위]`
피드에 전일 +15%↑ 종목에 `⚠️단기과열` 태그 자동 부착 + 중요7에 "⑧ 이 태그 있으면 직접재료
불확실 시 반드시 제외" 규칙 추가.

추가로 이 성적표 자체를 검증하다가 **직접 발견한** 별개 버그 2건:
- **`_scorecard_report`의 `if not _pf or not base:` 방어 코드가 죽은 코드** — `_price_full()`은
  실패해도 `(None,None,None,None,None)` "튜플"을 반환하는데 튜플은 항상 참(truthy)이라 이 체크가
  실패를 못 걸러냄 → `_cur=None`으로 이어져 나눗셈에서 크래시할 수 있는 잠재 위험(지금까진 우연히
  `_price_full`이 매번 성공해서 안 드러났을 뿐). `_cur is None`을 명시적으로 확인하도록 수정.
- **미해결 이상현상 발견(진단 로그만 추가)**: 실제 성적표에서 5종목(삼성전기·중앙에너비스·
  한화오션·대한광통신·우리기술)이 "현재가=추천가"로 원 단위까지 완전히 일치했는데, 같은 종목의
  장중 고/저는 실제로 크게 움직인 값(우리기술 저-16.6% 등)이었음 — 5종목이 우연히 정확히 같은
  값으로 마감했을 확률은 사실상 0이라 KIS API가 특정 상황에서 최신 체결가 대신 다른 값(기준가 등
    추정)을 반환하는 것으로 의심되나, 원본 API 응답 없이는 원인 확정 불가. `_metrics()`에 이 모순
  패턴(현재==추천가인데 고/저는 다름) 발생 시 진단 로그를 남기도록 추가 — 재현되면 콘솔에
  `[성적표 진단]` 줄이 뜰 것, 다음에 뜨면 캡처 필요.
- **반영 안 함**: 리뷰가 제안한 "진입가 별도 기록·손절 실제 체결 여부 추적" 시스템은 이 봇에
  브로커리지 주문 연동 자체가 없어(신호만 텔레그램 발송, 실제 매매는 사용자 수동) 사용자가 매번
  실제 체결가를 수동 입력해야 하는 workflow 추가가 필요한 큰 기능 — 지금 범위 밖으로 보류.

### 7.17 5차 외부 리뷰 검증 — GUI 역순표시 오인 + chg 폴백 사각지대(진짜 결함) 수정

사용자가 GUI "시장복기 보기"로 본 화면을 캡처해 공유, 5차 외부 리뷰가 "주간메타복기가 상단에
중복" 등을 재지적. 검증 결과 절반은 오인, 절반은 진짜 결함:
- ❌ **"주간메타복기가 파일 상단"은 GUI 표시 방식 때문** — `tools_gui.py`의 `_view_market_review()`가
  "최신이 위로 오게" 전체 블록을 **의도적으로 역순 표시**함(주석에 명시). 실제 파일(디스크)은
  7.13에서 만든 대로 일별 블록이 오름차순으로 위, 주간메타복기가 하단에 있음 — 화면 캡처만 보고
  파일 구조가 잘못됐다고 오인한 것. 코드 변경 불필요.
- ❌ **"[확인된 사실]에 해석 문장이 섞임" 예시 오류** — 리뷰가 인용한 "기타법인의 대규모
  순매수세가... 기여했을 가능성이 있습니다" 문장은 실제로는 09-01 블록의 **[시장 해석] 섹션 안에
  정확히 위치**함(원문 재확인함). 잘못 인용된 지적.
- ✅ **[실질 결함 발견] `_index_daily_range()` 조회 시작일이 딱 요청 범위 첫날이라, 그 배치의
  첫날은 '전일 종가'가 없어 7.12의 등락률 자체계산 폴백이 못 먹힘** — 사용자가 직접 검증한
  사례(09-08: 6,954.52, 전일 09-07: 6,995.39 → -0.58%가 계산 가능한데도 "미확인"으로 남음)로
  확인. `backfill_market_review`가 KIS 조회 시작일(`_d1`)을 요청 범위보다 **7일 앞당겨** 가져오도록
  수정 — 이제 배치 첫날도 항상 계산용 전일 종가를 확보(주말 껴도 7일이면 충분).
- (참고) 08-26·08-27·09-02·09-08·08-25·09-09(Wed 표기)·주간메타x2가 여전히 예전 형식/문구로
  남아있는 건 새 코드가 아직 그 날짜들을 재처리 안 했기 때문(실시간 일일복기는 하루 1회만 생성,
  backfill은 최근 N영업일 롤링 윈도우라 오래된 날짜는 윈도우 밖으로 밀려나면 재처리 대상에서
  영원히 빠짐). 주간메타복기 중복은 버튼 재실행으로 해소 가능하나, 이미 롤링 윈도우 밖으로 밀린
  과거 날짜(08-25 등)를 강제로 재생성하는 기능은 없음 — 실거래 판단에 쓰이는 건 최근 7일뿐
  (`_load_review_blocks(days_back=7)`)이라 우선순위 낮다고 판단, 별도 기능 추가는 보류.
- 4파일 분리 제안은 7.12/7.13과 동일 이유로 계속 보류(반복 설명 생략).

### 7.16 GUI 버튼 정리 — 자사주테스트 제거, 청산분석을 신호분석에 병합

7.15 감사 결과를 본 사용자 요청으로 버튼 12개→11개로 정리:
- **🔄 자사주 반전 테스트 버튼 삭제** — 코드 자체 주석에도 "파이프라인 확인용"이라고 돼 있는
  개발자 QA 도구라 일상 트레이딩 판단엔 안 쓰임. CLI(`--test-buyback`)는 남겨둠(수동 디버깅용).
- **⏱️ 종배 청산분석 버튼 삭제 → 📊 신호분석에 병합**(`--analyze` 실행 시 `_analyze_history` 직후
  `_analyze_exit_timing`도 자동 호출, 텔레그램 2건 연속 발송). 버튼 라벨도 "신호분석(+청산분석)"으로
  변경. `--exit-analysis` CLI 플래그·함수 자체는 그대로 유지(단독 실행도 가능).

### 7.15 GUI 버튼(퀀트 트레이딩 도구) 알고리즘 전수 감사 — "정밀 검진" 요청 대응

`tools_gui.py`의 12개 버튼이 부르는 macro_watcher.py 함수를 전부 다시 훑어 실제 버그 2종을 발견·수정:

**① "어제" = 달력상 하루 전(date-1)으로 계산 — 월요일마다 금요일 기록을 못 찾는 버그**
`_scorecard_report`(📋성적표)·`check_gap_analysis`(갭분석)·프리마켓 유니버스 빌더 3곳이 전부
`(now_kst - timedelta(days=1))`로 "어제"를 구해 그 날짜의 종배·브리핑 기록을 찾았음. **월요일에
실행하면 "어제"가 일요일이 되어, 실제로 봐야 할 금요일 기록을 못 찾고 매번 "기록 없음"으로 빈
결과가 나옴**(공휴일 다음날도 동일 문제, 다만 빈도는 낮음). `_prev_trading_day_str(now_kst)`
공용 헬퍼를 신설(주말만 건너뛰고 직전 평일 반환 — 공휴일까지는 트레이딩 캘린더 없이 불가해 미대응)
해서 3곳 모두 교체.
- (참고) `send_daily_review`의 코스피200 야간선물 표시부(`_yday`, 라인 5921 부근)는 정확한
  거래일 조회가 아니라 "데이터가 today/어제 중 하나면 유효"라는 느슨한 신선도 체크라 그대로 둠.

**② `_investor_est()` — API 조회 실패와 "실제 순매수 0"을 구분 못 하던 버그 (6곳 영향)**
이 함수는 외국인·기관 추정 순매수를 반환하는데, **API 호출이 실패해도 예외를 삼키고 `(0, 0)`을
반환**했음. 그런데 코드 6곳이 "혹시 실패하면 None이 오겠지"라고 가정하고 `if _f is None or _o is
None:` 같은 방어 체크를 이미 심어뒀었음 — 이 체크들은 전부 **죽은 코드**였고(`_f`/`_o`가 실제로는
절대 None이 될 수 없었음), 실패 시 "확인된 수급 0(중립 또는 유입)"으로 잘못 표시됐음. 구체적으로:
- `check_dolpanty_pick`(종배 확정픽) — V24.9에서 "조회 실패를 조용히 이상무로 넘기지 말자"고
  명시적으로 남긴 주석의 그 로직이 애초에 발동한 적이 없었음.
- `_elite_tag`(⭐정예 태그) — 독스트링에 "당일 수급 미확인 → 정예 아님"이라고 써놓고 실제로는
  조회 실패해도 정예 태그가 그대로 붙는 정반대 동작.
- `_holding_judge`(보유 8시 매도/홀딩 판정) + 실시간 애프터마켓 판정부(중복 로직) — 조회 실패가
  "✅유입"으로 표시돼 보유자에게 거짓 안심을 줄 수 있었음(보유 판정이라 특히 민감).
- `_deep_stock`(종목해석)·`_volatility_scan`(변동성 스캐너) — 조회 실패가 "수급 0억✅"로 표시.
- **수정**: `_investor_est(..., distinguish_fail=False)` 옵션 파라미터 추가 — 기본값은 기존과
  100% 동일(다른 11개 호출부는 원래부터 `(_fe or 0)` 패턴으로 실패=0을 의도한 설계라 안 건드림),
  `distinguish_fail=True`로 부르면 실패 시 `(None, None)`을 반환해 위 6곳의 기존 None-체크가
  드디어 실제로 작동하게 함.

**그 외 확인만 하고 안 건드린 것**: `_regime_detect`(장세판독, 중앙값 기반이라 견고함),
`test_buyback_scan`(자사주테스트, 시세없음 처리 정상), `_holdings_report`의 전일종가 산출(라이브
API 응답 형태를 실측 없이는 확정 못 해 보류) — 명확한 증거 없이 추측성 수정은 하지 않음.
`_analyze_exit_timing`(청산분석)에는 `--analyze`와 같은 표본 10건 미만 경고 라벨을 일관되게 추가.

### 7.14 야후파이낸스 전면 실패 시 "리스크오프"와 "데이터 장애" 혼동 수정 (실제 콘솔 로그로 발견)

사용자가 공유한 실제 콘솔 로그: `NQ=F/^SOX/NVDA/AVGO/MU/CL=F/EWY/USDKRW=X` 전종목이 동시에
"possibly delisted"로 실패 → `compute_macro()`는 이미 이 전체결측 상황을 `data_state="outage"`로
정확히 구분해서 반환하고 있었는데(V24.9 fail-safe, sev=2로 보수적 차단 자체는 설계대로 정상 동작),
**`check_dolpanty_pick()`가 `sev`만 받고 `data_state`는 두 호출부(`--dolpanty` 강제실행, 메인루프)
모두에서 버려서(`_sev, _, _, _ = compute_macro(...)`)**, 텔레그램엔 항상 "오늘은 리스크오프"라고만
나가 실제로는 "데이터를 못 가져와서 판단 자체가 불가능한 상태"를 "시장이 실제로 위험하다"로
오인하게 만드는 메시지였음. NVDA까지 "delisted" 뜬 것 자체가 실제 상장폐지가 아니라 야후 쪽
일시적 네트워크/레이트리밋 문제라는 확실한 증거.
- **수정**: `check_dolpanty_pick()`에 `data_state` 파라미터 추가, 두 호출부 모두 버리던 4번째
  반환값을 그대로 전달. `sev==2`일 때 `data_state=="outage"`면 "매크로 지표 조회 실패로 판단
  보류 — 실제 리스크오프 여부 불명, 데이터 소스 장애로 종가베팅 스킵"으로 메시지 분기(기존 "오늘은
  리스크오프" 문구는 진짜 리스크오프일 때만).
- **`_pct()`/`_hist_pct()`에 1회 재시도 추가** — 전종목 동시 실패 패턴은 일시적 네트워크 문제일
  가능성이 있어 짧은 재시도로 완화(단, Yahoo 쪽 세션 자체가 차단된 경우엔 재시도도 안 먹힐 수 있음 —
  근본 해결 아님, 완화책).
- **Gemini 모델 폴백에 `gemini-2.0-flash` 추가** — 같은 로그의 `[Gemini 진단]`에서
  `gemini-2.5-pro:NotFound`(쿼터와 무관한 영구 실패, 모델명 자체가 이 계정에서 안 잡힘) 확인 →
  더 널리 쓰이는 안정 모델을 폴백에 추가(`gemini-2.5-flash:ResourceExhausted`는 오늘 테스트로 인한
  일시적 쿼터 소진으로 추정, 자정 이후 리셋되면 자연 해소).
- **반영 안 함**: 대체 데이터소스(investpy/alpha_vantage 등) 추가는 새 API 키 발급·심볼 매핑까지
  필요한 큰 통합 작업이라 지금은 보류(yfinance는 무료·키 불필요라 실사용 검증 없이 이관 시 이득 대비
  리스크 큼). yfinance 버전 업그레이드는 requirements.txt에 이미 버전 미고정(항상 최신 설치)이라
  저장소 쪽에서 고칠 게 없음 — 로컬 환경이 오래된 캐시 버전이면 사용자가 직접
  `pip install --upgrade yfinance` 필요.

### 7.13 market_review.md 날짜순 재정렬 구조로 전환 (4차 외부 리뷰 — 이번엔 실제 결함)

4차 외부 리뷰가 실제 파일 순서(9/7→9/3→9/1→8/31→...→9/8→9/4→9/2→9/9→주간x2→8/25)를 근거로
"파일이 날짜순이 아니라 실행 순서로 쌓인 로그"라고 지적. 이번엔 재확인 결과 **실제 결함**이었음
(7.12의 "요일 불일치"와 달리 "정렬 순서"는 코드가 정말 append-only였음). 다만 리뷰가 제안한
market_data_ledger.csv/rules_candidates.md/weekly_review.md 4파일 분리는 여전히 과설계라 판단해
반영 안 하고, 핵심 문제("파일 내 정렬")만 최소 변경으로 해결:
- **`_mr_read_all()`/`_mr_write_all()` 신설** — market_review.md를 "일별 블록 dict{날짜: 본문} +
  기타 블록(주간메타복기 등) list"로 파싱/재작성하는 공용 함수. 일별 블록은 항상 날짜 오름차순으로
  위에, 기타 블록은 아래에 몰아서 씀. `backfill_market_review`/`send_daily_review`의 일별 복기
  저장부/`weekly_meta_review` 저장부 3곳 전부 이 공용 함수로 통일 — 이제 어떤 순서로 실행해도
  파일 자체가 항상 날짜순으로 재정렬됨(다음 실행 때 기존 파일까지 통째로 다시 정렬하므로 지금까지
  쌓인 뒤죽박죽 상태도 다음 backfill/일일복기 실행 한 번으로 자동 정리됨).
- **"# ── 과거 backfill N일(생성 ...) ──" 구분줄 폐지** — 정렬에 쓸 날짜 정보가 없는 생성이력일
  뿐이고, 각 "## 날짜" 블록이 이미 자기 날짜를 갖고 있어 없어도 정보 손실 없음. 기존 파일에 남아있는
  이 구분줄은 `_mr_read_all()`이 파싱 시 제거(다음 재작성부터 사라짐).
- 이제 필요 없어진 `_remove_review_blocks()`(날짜 지정 삭제 함수)는 `_mr_read_all`의 dict 덮어쓰기가
  같은 역할을 자동으로 해서 삭제.
- **검증**: 실제 파일과 동일한 구조(구분줄이 블록 끝에 붙어있는 경우 포함)를 sandbox에서 재현해
  `_mr_read_all`/`_mr_write_all` 단독 실행 — 날짜 오름차순 정렬, 구분줄 제거, 같은 날짜 자동 dedup
  모두 정상 동작 확인.
- **반영 안 함(이유 동일)**: 4파일 분리, CSV 원장, 20건 표본 하드게이트 — 7.12와 같은 이유(규칙
  후보는 이미 사람이 수동 검토, 큰 리팩터 대비 이득 불분명)로 보류.

### 7.12 market_review.md 3차 외부 리뷰 — 등락률 자체계산 도입 + 사실/해석 분리 강화

7.10에서 공유받은 실제 파일에 대해 또 다른 외부 리뷰가 "날짜·요일 불일치·중복·사실과 추정 혼합·가설의
규칙화"를 지적하며 4개 파일 분리(market_data_ledger.csv/market_review.md/rules_candidates.md/
weekly_review.md) 같은 큰 구조 개편을 제안. 검증 결과 일부는 사실이 아니었고, 일부는 이미 안전장치가
있었고, 실제 가치 있는 지적 1개는 코드로 반영:
- ❌ **"요일 불일치" 재확인 결과 전부 사실 아님** — 리뷰가 지목한 09-07(월)·09-03(목)·08-31(월)·
  08-26(수) 등 원장에 찍힌 요일을 Python `datetime.weekday()`로 전부 재계산해 대조 — **하나도 틀리지
  않음**(전에도 Perplexity가 같은 종류의 오류 주장을 했다가 틀렸던 전례와 동일 패턴). 애초에 이 요일들은
  Gemini 추정이 아니라 `_WKD_KO[_dd.weekday()]`로 코드가 계산해서 박아넣는 값이라 틀릴 수가 없음.
  유일하게 진짜였던 흠은 `send_daily_review`의 텔레그램 헤더 2곳이 아직 `strftime('%a')`(영어 Mon/Wed)를
  쓰고 있던 것 — `_WKD_KO`로 통일(market_review.md 저장부는 이미 그전에 고쳐져 있었음).
- ✅ **[실질 반영] 등락률(chg)을 KIS 필드값 대신 연속 종가로 직접 계산하는 폴백 추가** — `bstp_nmix_prdy_ctrt`
  필드명이 맞는지 여전히 미검증인 채로 사용자를 기다리게 하는 대신, `_index_daily_range()`가 이미
  받아온 날짜range의 연속 종가로 `(오늘종가/전일종가-1)*100`을 직접 계산해 채워 넣도록 함. 필드명 문제와
  완전히 무관하게 정확한 값을 즉시 얻을 수 있음(range의 첫날만 전일 종가가 없어 계산 불가 — 그건 정상).
- ✅ **[실질 반영] 사실/해석 혼합 방지 강화** — 실제 09-07 복기에서 Gemini가 KIS가 "미확인"으로 남긴
  등락률(4.61%)을 자기가 검색해서 채워 넣어 서론에 재서술한 걸 확인(리뷰의 "사실과 추정 혼합" 지적이
  실제로 여기서 재현됨). backfill 프롬프트에 "서론 금지·이미 준 수치 재언급/재계산 금지·[시장 해석]
  [다음 참고] 두 섹션만 출력" 지시 추가.
- ⛔ **[반영 안 함, 이유 명시] 4파일 구조 분리·"최소 20개 사례 검증 전 규칙화 금지" 하드게이트** —
  거절 사유: 지금도 `weekly_meta_review`가 만드는 "규칙 후보"는 `rules_ledger.md`에 자동 반영되지
  않고 사람이 검토 후 수동 반영하는 구조라(7.1) 리뷰가 우려하는 "검증 안 된 가설이 실거래 규칙으로
  자동 승격"되는 실질적 위험은 이미 차단돼 있음. CSV/가설파일/주간파일 분리는 모든 읽기·쓰기 지점
  (`_remove_review_blocks`·backfill·weekly_meta_review·대시보드 --analyze)을 다시 설계해야 하는
  큰 리팩터라, 이미 있는 안전장치 대비 이득이 불분명해 보류. 09-03/08-26/08-31 기존 항목에 "데이터
  품질 경고" 태그를 소급으로 붙이는 것도 사용자 로컬 파일을 직접 건드릴 방법이 없어 미반영(코드는
  이제 사실/해석을 더 잘 분리하니 새로 쌓이는 항목부터 개선됨).
- (참고) "9/4·주간메타복기 중복" 중 주간메타복기 중복은 7.6 수정 전 잔재로 이미 설명됨. 9/4 중복은
  이번에 공유받은 내용에서는 확인 안 됨(한 번만 등장) — 실제 파일에 중복이 있다면 그 부분을 다시
  캡처해서 공유 요청.

### 7.11 저녁뉴스 재료등급 3차 정교화 (실제 브리핑 외부 리뷰 반영)

실제 발송된 브리핑을 본 외부 리뷰에서 "구글 AI 데이터센터 투자 → 대한광통신 A급 추천", "두산퓨얼셀(9/2
구뉴스) → 우리기술(9/9 급등) 재료로 갖다붙임", "확신 없는데도 TOP3 억지로 채움" 3가지 패턴 지적.
기존 중요7의 ①~④(ETF편입·특수관계인 장외거래·해외투자라운드·유가일괄테마) 규칙으로는 안 걸러지는
새로운 과대평가 패턴이라 판단해 반영:
- **⑤ 해외 대기업 인프라투자→국내 종목 자동연결 금지** — 구글/MS/아마존 등의 데이터센터·AI·클라우드
  투자 발표는 국내 종목의 직접 납품·공급계약이 공시·IR로 확인 안 되면 A/A- 금지(B 이하).
- **⑥ 종목 간 재료 갖다붙이기 금지** — B종목 추천 근거로 무관한 A종목의 뉴스를 쓰지 못하게 명시
  (두산퓨얼셀 뉴스로 우리기술 설명한 실제 오류 케이스).
- **⑦ 뉴스 시각 기반 신선도 반영** — 이미 각 뉴스 줄에 있는 [출처 시각] 태그를 활용해, 3거래일↑ 지난
  뉴스는 '오늘의 신규 트리거'로 못 쓰게 명시(지속성 참고용으로만 등급 하향).
- **TOP3 문구 수정** — "딱 3개만" → "최대 3개" + "고확신 0개면 그렇게 써라, 억지로 채우지 마라"를
  명시적으로 추가(리뷰가 지적한 "추천 0개도 정상 결과" 원칙을 프롬프트에 직접 반영).
- **반영 안 한 것(의도적, 문서화)**: 리뷰가 제안한 "뉴스 범위 재확인" 4단계 표(0~24h/2~5일/6~30일/30일+)와
  "테마 혼합 금지" 별도 블록은 통째로 넣지 않음 — 이미 길어진 중요7 규칙 안에 핵심 판단기준(⑤⑥⑦)을
  간결하게 흡수시키는 걸로 충분하다고 판단(프롬프트 비대화·중복 방지). "네이버 많이 본 뉴스는 사실등급
  근거로 쓰지 마라"는 이미 7.4/중요6에서 구현 완료 — 이번 리뷰 제안과 사실상 동일해 변경 없음(검증됨).

### 7.10 실제 market_review.md 결과물 분석 — 2건 수정 + 1건 확인 필요(사용자 액션)

먹통 수정 후 사용자가 재실행에 성공해 실제 market_review.md 전체를 공유. 확인된 것:
- ✅ 크래시(7.8)·먹통(7.9) 둘 다 재발 안 함 — "등락률 미확인"으로 안전하게 표시되며 끝까지 완주.
- ⚠️ **모든 날짜(11일치 전부)에서 등락률이 "미확인"** — close(`bstp_nmix_prpr`)는 매번 파싱되는데
  chg(`bstp_nmix_prdy_ctrt`)는 단 한 건도 파싱 안 됨. 일부만 실패하는 게 아니라 전건 실패라는 건
  간헐적 결측이 아니라 **필드명 자체가 이 엔드포인트에서 다르다는 신호**. 7.8에서 추가한 진단 로그
  (`[지수일별시세 진단] close는 있는데 등락률(chg) None N건 ... 필드명 확인 필요: [...]`)가 콘솔에
  찍혔을 텐데 이번 공유엔 market_review.md만 있고 콘솔 로그가 없어서 실제 필드 키 목록을 아직
  못 봤음. **다음 실행 때 콘솔에 이 줄이 뜨면 그 옆 필드 키 리스트를 캡처해서 공유 필요** —
  그래야 `bstp_nmix_prdy_ctrt` 대신 어떤 키를 써야 하는지 확정 가능(추측으로 필드명 변경 안 함).
- 🐛 **08-28 블록에 지식컷 착각 오류가 다른 문구로 또 남아있었음** — "실시간 미래 데이터 검색
  불가·정보 접근 한계로 확인 불가"라는 표현은 기존 `_FUTURE_CONFUSION_MARKERS`(예: "아직 도래하지
  않") 어느 것과도 매칭 안 돼 필터를 통과해 저장됨. 마커 목록에 "실시간 미래 데이터"·"미래 데이터
  검색"·"정보 접근 한계" 추가.
- 🐛 **backfill 헤더 날짜가 KST 아닌 로컬 `datetime.now()` 사용** — 실제로 "생성 2026-09-10"
  헤더가 찍힌 게 확인됨(그 시점 KST 기준 오늘은 09-09). 함수 내부에서 이미 계산해둔 KST 기준
  `_now`로 통일.
- (참고) 주간메타복기 블록이 2개 중복(7.6 수정 이전에 만들어진 잔재) — backfill은 주간메타복기
  블록을 건드리지 않으므로 자동으로는 안 없어짐. `주간 메타복기` 버튼을 한 번 더 누르면 최신 코드가
  같은 주(2026-09-01~07) 블록을 지우고 하나로 교체함.

### 7.9 backfill "먹통"(무응답 정지) 수정 — 신SDK 클라이언트 타임아웃 누락

7.8 수정 후 사용자가 재실행했더니 크래시는 없어졌는데 이번엔 `▶ 과거복기 실행...`에서
**그대로 멈춰서 응답이 없는(먹통)** 증상 보고. 원인: `_gemini_stock_news_verdict`와
`_gemini_grounded` 둘 다 신SDK(`google-genai`) 경로에서 `genai.Client(api_key=gkey)`를
**타임아웃 설정 없이** 생성 — 구SDK 폴백 경로는 `request_options={"timeout": 60}`을 항상
넣었는데 신SDK 경로만 빠져 있었음. 네트워크가 응답을 안 주는 상황(방화벽/프록시가 조용히
패킷을 드롭하는 등)에서 이 호출이 **영구 대기**해 backfill 루프 전체가 멈춰버림(최대 10일치
반복 호출 중 한 번만 걸려도 전체 정지).
- **수정**: 두 곳 모두 `genai.Client(api_key=gkey, http_options=types.HttpOptions(timeout=60_000))`
  로 60초 타임아웃 강제(ms 단위, 구SDK 경로와 동일 기준). sandbox에서 `google-genai` 패키지를
  임시 설치해 `HttpOptions(timeout=...)` 파라미터가 실제로 존재/동작함을 확인 후 반영.
- **참고**: `_gemini_grounded`의 429/쿼터 재시도(`time.sleep(12)`)는 원래도 유한 대기라 문제 아님 —
  먹통의 원인은 그 이전 단계인 HTTP 요청 자체의 무한 대기였음.

### 7.8 backfill 실제 실행 크래시 수정 — KIS chg 필드 None 처리 (실제 트레이스백으로 발견)

사용자가 실키로 `과거복기 학습(10일)` 버튼을 재실행해 처음으로 진짜 크래시 로그를 확보:
`[backfill] KIS 지수 실측 11일(코스피)/11일(코스닥) 확보`까지는 정상 출력됐는데 그다음
`TypeError: unsupported format string passed to NoneType.__format__`로 죽음. 원인:
`_index_daily_range()`의 내부 헬퍼 `_one(nm, s)`가 `s.get("close") is not None`만 검사하고
`s['chg']`는 무조건 포맷 문자열에 꽂았는데, 실측 데이터 중 **close는 파싱됐지만 chg(등락률,
`bstp_nmix_prdy_ctrt`)만 None인 날짜가 실제로 존재**함(5.7/7.2에서 우려했던 "KIS 필드명
미검증" 리스크가 처음으로 실증됨 — close 필드는 맞는데 등락률 필드는 특정 날짜에 비거나
다른 이름일 가능성).
- **수정**: `_one()`이 `chg`가 None이면 크래시 대신 "등락률 미확인"으로 표시하도록 방어.
  + `_index_daily_range()`에 `close는 있는데 chg만 None`인 건수를 세는 진단 로그 추가
  (기존 "파싱 0건" 진단과 같은 패턴) — 다음 실행 로그에서 몇 건이나 이 케이스인지, 그리고
  원본 필드 키 목록이 찍히므로 `bstp_nmix_prdy_ctrt`가 맞는 필드명인지 바로 확인 가능.
- **다음 확인 사항**: 버튼을 한 번 더 눌러서 "등락률 미확인" 진단 로그가 몇 건 찍히는지,
  그리고 그 필드 키 목록에 실제 등락률에 해당할 만한 다른 이름이 있는지 확인 필요.

### 7.6 주간 메타복기 중복 저장 버그 수정 (실제 market_review.md 캡처로 발견)

사용자가 공유한 실제 market_review.md에 **"📊 주간 메타복기 (2026-09-01~2026-09-07, 5일치)"가
토씨만 다르게 통째로 2번** 들어있는 게 확인됨. 원인 2가지:
1. `--weekly-review`(GUI "📊 주간 메타복기" 버튼) 처리부가 `load_state()` 결과를 변수에 담지 않고
   바로 넘긴 뒤 **`save_state()`를 호출 안 함** → `weekly_meta_review()` 내부에서 `state["weekly_review_week"]`를
   세팅해도 디스크에 전혀 저장이 안 돼, 버튼을 다시 누르면 "이번 주 이미 만들었음"을 기억 못 함.
2. 애초에 `force=True`(수동 테스트) 경로는 요일·시간창·중복 체크를 통째로 건너뛰게 설계돼 있어서,
   1번을 고쳐도 버튼을 두 번 누르면 어차피 또 새로 만듦 — **저장 방식 자체가 append-only**라
   같은 주 콘텐츠가 계속 쌓이는 구조였음.
- **수정**: `--weekly-review` 디스패치에서 `save_state()` 호출 추가. + `weekly_meta_review()`의 파일
  저장부를 `backfill_market_review`의 `_remove_review_blocks`와 같은 패턴으로 바꿔, **같은 날짜범위
  (`_start~_end`)의 기존 주간메타복기 블록을 지우고 교체**하도록 함(append→덮어쓰기). 이제 버튼을
  몇 번을 눌러도 그 주 요약은 항상 최신 1개만 유지됨. 겸사겸사 이 함수의 파일쓰기도 원자적 쓰기
  (`_atomic_write_text`)로 통일(7.3에서 놓쳤던 append 모드 쓰기였음).
- **아직 안 고친 것(사용자 액션 필요)**: 같은 캡처에 `2026-08-26`·`2026-08-31` "미래 날짜 착각" 오류
  항목이 여전히 그대로 남아있음 — 이건 7.4/이전 세션에서 코드는 고쳤지만, **backfill을 실제로
  재실행해야 파일 내용이 교체됨**(일일 자동복기는 오늘 날짜만 추가하지 과거 날짜를 안 건드림).
  `과거복기 학습(10일)` 버튼을 한 번 더 눌러야 이 두 항목이 정상 내용으로 교체됨.

### 7.7 `--analyze` 신호별 성적 첫 실사용 데이터 분석 (뉴스→종목선정→실제 결과)

사용자가 실제로 몇 주 돌린 `--analyze` 결과(신호별 승률/평균수익)를 공유. 뉴스(재료)→종목선정(점수화)→
실제 결과(익일 종가) 흐름으로 보면:
- **🎯 브리핑 65건 · 60% · +1.5%** — 표본도 크고 결과도 좋음. 이번 세션 초반에 종배 스코어링에서
  브리핑 겹침 가중치를 25→35로 올린 결정이 데이터로 검증됨(재료·뉴스 선행 신호가 거래대금 후행 신호보다 강함).
- **진입 12건·67%·+2.4%**, **시가저격 8건·75%·+1.4%** — 당일단타 계열도 견조.
- **종배그림자 56건·52%·+0.0%** — 이건 애초에 **텔레그램 발송 안 되는 벤치마크(대조군)**임
  (`_log_shadow()`가 "뽑힐 뻔한 후보"를 조용히 기록만 함). 대조군이 딱 본전(0%)인 게 정상이고,
  오히려 실제 발송 신호(브리핑/진입/시가저격)가 이 대조군보다 확실히 낫다는 걸 보여주는 **검증 데이터**
  — 끄거나 손댈 대상 아님.
- **종배픽(NXT) 6건·50%·-2.1%**, **종배분산 6건·33%·-1.9%** — 부진하지만 표본 6건은 노이즈에
  쉽게 뒤집힘. 코드를 재확인한 결과, `check_dolpanty_pick`의 점수 계산 단계(V25.42)에서 재료
  미확인(ng not in S/A) 후보는 이미 **점수 상한 49점**으로 캡되어 원톱(`pick`)이든 분산(`div`)이든
  발송 임계(50/60점)를 넘을 수 없음 — 즉 "분산만 재료검증을 건너뛴다"는 가설은 틀림(이미 둘 다
  동일하게 재료 게이트를 통과해야 함). 두 변형이 비슷하게 부진한 건 분산 로직 결함이 아니라
  **종배(오버나이트 보유) 자체가 현재 장세에서 당일단타 대비 약하다**는 신호에 가까움 — 표본 20건
  이상 쌓일 때까지 지켜보고, 그래도 마이너스면 그때 `급증진입`(V24.7, 0%·-6.2%로 실행중단된 전례)
  처럼 끄는 걸 검토.
- **15분봉 5건·20%·+0.9%**, **공시발굴 4건·50%·+6.1%**, 나머지 1건짜리 3종 — 표본 1~5건은
  통계적으로 판단 불가(우연 편차 구간). 지금 액션 트리거로 쓰면 안 됨.
- **코드 변경**: `_analyze_history()` 출력에 **표본 10건 미만 자동 라벨(⚠️표본작음)** 추가 — 사용자가
  "승률↓·평균− → 실행중단" 기준으로 이 리포트를 쓰겠다고 명시했는데, 절반 이상의 신호가 1~6건이라
  소표본 노이즈를 성급하게 실행중단 근거로 오용할 위험이 있어 리포트 자체에 경고를 박아둠.
- (보류) `과거복기 학습(10일)` 버튼 재실행 후 GUI "실행 로그" 캡처 요청은 아직 회신 대기 중 —
  KIS 지수 실데이터 필드명 검증 및 08-26/08-31 항목 정상화 여부 확인용.

### 7.1 주간 메타복기 구현 메모 (이번 세션)

- `weekly_meta_review(now_kst, state, token_tg, chat_id, gemini_key, force=False)` — 매주 **금요일 15:35~15:50**(마감복기 시점) 자동 1회 실행.
  `market_review.md`에서 최근 7일 이내 `## YYYY-MM-DD...` 일일 블록(직전 주간메타복기 블록은 재료로 재사용 안 함)을 모아 Gemini에게
  ①반복 패턴·테마 ②실제로 잘 먹힌 신호/조건 ③다음 주 포커스 ④`rules_ledger.md` 반영 검토용 규칙 후보 를 물어 텔레그램 발송 +
  `market_review.md`에 `## 📊 주간 메타복기 (시작~끝)` 블록으로 누적.
- 누적 일수가 3일 미만이면 스킵(데이터 부족), 같은 주(ISO week)에 이미 생성됐으면 중복 스킵(`state["weekly_review_week"]`).
- 수동 테스트: CLI `--weekly-review`(요일/시간창/중복체크 무시하고 즉시 실행) + GUI(`tools_gui.py`) "📊 주간 메타복기" 버튼.
- rules_ledger.md는 **자동으로 쓰지 않음** — Gemini가 제안한 규칙 후보는 텔레그램/market_review.md에서 사람이 검토 후 수동 반영(기존 규칙원장 운영 방식 유지).

### 7.2 시장복기 신뢰도 개선 (외부 리뷰 반영, 이번 세션)

외부(Perplexity) 리뷰에서 "market_review.md가 원장이 아니라 AI 생성 시황문에 가깝다"는 지적을 받고 반영:
- **`_index_daily_range()` 신설** — KIS 지수 일별시세(`inquire-daily-indexchartprice`, FHKUP03500100)로 코스피/코스닥의 **실제 종가·등락률**을 가져와 backfill에 사용. 이제 숫자는 Gemini 추측이 아니라 KIS 실측(`[확인된 사실](KIS 실측)` 태그로 구분). KIS 응답에 해당 날짜가 아예 없으면 **휴장으로 확정 처리**(Gemini 호출 자체를 생략) — "미수집"과 "휴장"을 혼동하지 않도록.
- ⚠️ **필드명 미검증** — 5.7번 한계 참조. 실키로 첫 backfill 실행 시 콘솔 로그 꼭 확인.
- KIS 키 없으면 예전처럼 Gemini 단독 추정으로 자동 폴백(신뢰도 낮음 명시).
- 모든 시장복기 프롬프트(당일/backfill/주간메타복기)에 "확인된 사실 vs 시장 해석" 분리 + 단일 원인 단정 금지(가능성 언어 강제) 지시 추가.
- backfill이 과거 날짜를 "아직 안 온 미래"로 착각하는 Gemini 지식컷 오류를 `_is_future_confused()`로 감지해 스킵.
- (보류) Perplexity가 제안한 8단계 풀 구조화 표(수급 억원 단위·거래대금·재현 IF-THEN 표 등)는 미구현 — 수급/거래대금까지 실측 연동한 뒤에 재검토.

---

## 8. 운영 메모

- 실행: KIS App키/시크릿 + 텔레그램 토큰/챗ID + Gemini키 + DART키(secrets.toml 또는 환경변수). GUI(tools_gui.py)로 감시 시작/중지.
- Windows cmd 한글 인코딩 이슈로 GUI 앱 선호. stdout utf-8 reconfigure 되어있음.
- 원칙(사용자 룰): 소액·분할·−2~3% 칼손절·존버 금지·몰빵 금지.
